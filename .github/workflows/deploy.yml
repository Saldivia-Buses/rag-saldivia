name: Deploy RAG Saldivia

on:
  # Deploy al hacer tag vX.Y.Z (releases)
  push:
    tags:
      - 'v*'
  # Deploy manual desde GitHub Actions UI
  workflow_dispatch:
    inputs:
      profile:
        description: 'Deployment profile'
        required: false
        default: 'workstation-1gpu'
      workstation:
        description: 'Target workstation label'
        required: false
        default: 'workstation-1'

jobs:
  deploy:
    name: Deploy to workstation
    runs-on: [self-hosted, "${{ github.event.inputs.workstation || 'workstation-1' }}"]
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Deploy
        run: make deploy PROFILE="${{ github.event.inputs.profile || 'workstation-1gpu' }}"

      - name: Health check
        run: make status

      - name: Notify result
        if: always()
        run: |
          STATUS="${{ job.status }}"
          SHA=$(echo "${{ github.sha }}" | cut -c1-8)
          REF="${{ github.ref_name }}"
          echo "Deploy $STATUS — ref=$REF sha=$SHA"
