// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tools.sql

package repository

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listTools = `-- name: ListTools :many
SELECT id, tenant_id, legacy_id, code, article_code, article_id,
       inventory_code, name, characteristic, group_code, tool_type,
       status_code, purchase_order_no, purchase_order_date,
       delivery_note_date, delivery_note_post, delivery_note_no,
       supplier_code, pending_oc, observation, manufacture_no,
       generated_at, created_at
FROM erp_tools
WHERE tenant_id = $1
  AND ($4::INTEGER = -1 OR status_code = $4::INTEGER)
  AND ($5::TEXT = '' OR article_code = $5::TEXT)
ORDER BY name, code
LIMIT $2 OFFSET $3
`

type ListToolsParams struct {
	TenantID      string `json:"tenant_id"`
	Limit         int32  `json:"limit"`
	Offset        int32  `json:"offset"`
	StatusFilter  int32  `json:"status_filter"`
	ArticleFilter string `json:"article_filter"`
}

// Serialized tool/item catalog. Filters by status and optional article code.
func (q *Queries) ListTools(ctx context.Context, arg ListToolsParams) ([]ErpTool, error) {
	rows, err := q.db.Query(ctx, listTools,
		arg.TenantID,
		arg.Limit,
		arg.Offset,
		arg.StatusFilter,
		arg.ArticleFilter,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ErpTool{}
	for rows.Next() {
		var i ErpTool
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.LegacyID,
			&i.Code,
			&i.ArticleCode,
			&i.ArticleID,
			&i.InventoryCode,
			&i.Name,
			&i.Characteristic,
			&i.GroupCode,
			&i.ToolType,
			&i.StatusCode,
			&i.PurchaseOrderNo,
			&i.PurchaseOrderDate,
			&i.DeliveryNoteDate,
			&i.DeliveryNotePost,
			&i.DeliveryNoteNo,
			&i.SupplierCode,
			&i.PendingOc,
			&i.Observation,
			&i.ManufactureNo,
			&i.GeneratedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getTool = `-- name: GetTool :one
SELECT id, tenant_id, legacy_id, code, article_code, article_id,
       inventory_code, name, characteristic, group_code, tool_type,
       status_code, purchase_order_no, purchase_order_date,
       delivery_note_date, delivery_note_post, delivery_note_no,
       supplier_code, pending_oc, observation, manufacture_no,
       generated_at, created_at
FROM erp_tools
WHERE id = $1 AND tenant_id = $2
`

type GetToolParams struct {
	ID       pgtype.UUID `json:"id"`
	TenantID string      `json:"tenant_id"`
}

func (q *Queries) GetTool(ctx context.Context, arg GetToolParams) (ErpTool, error) {
	row := q.db.QueryRow(ctx, getTool, arg.ID, arg.TenantID)
	var i ErpTool
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.LegacyID,
		&i.Code,
		&i.ArticleCode,
		&i.ArticleID,
		&i.InventoryCode,
		&i.Name,
		&i.Characteristic,
		&i.GroupCode,
		&i.ToolType,
		&i.StatusCode,
		&i.PurchaseOrderNo,
		&i.PurchaseOrderDate,
		&i.DeliveryNoteDate,
		&i.DeliveryNotePost,
		&i.DeliveryNoteNo,
		&i.SupplierCode,
		&i.PendingOc,
		&i.Observation,
		&i.ManufactureNo,
		&i.GeneratedAt,
		&i.CreatedAt,
	)
	return i, err
}

const listToolMovements = `-- name: ListToolMovements :many
SELECT id, tenant_id, legacy_id, tool_id, tool_code, user_code,
       quantity, movement_date, concept_code, created_at
FROM erp_tool_movements
WHERE tenant_id = $1 AND tool_id = $2
ORDER BY movement_date DESC NULLS LAST, legacy_id DESC
LIMIT $3 OFFSET $4
`

type ListToolMovementsParams struct {
	TenantID string      `json:"tenant_id"`
	ToolID   pgtype.UUID `json:"tool_id"`
	Limit    int32       `json:"limit"`
	Offset   int32       `json:"offset"`
}

// Lending ledger for a single tool (by tool_id). Returns the movement
// history most-recent first.
func (q *Queries) ListToolMovements(ctx context.Context, arg ListToolMovementsParams) ([]ErpToolMovement, error) {
	rows, err := q.db.Query(ctx, listToolMovements,
		arg.TenantID,
		arg.ToolID,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ErpToolMovement{}
	for rows.Next() {
		var i ErpToolMovement
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.LegacyID,
			&i.ToolID,
			&i.ToolCode,
			&i.UserCode,
			&i.Quantity,
			&i.MovementDate,
			&i.ConceptCode,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
