# Blueprint clone (not tracked — cloned by setup.sh)
blueprint/

# Stack anterior (Python + SvelteKit) — preservado localmente, fuera del repo
legacy/

# Secrets — nunca al repo
.env.local
.env.merged
.env.blueprint
.env.build
*.key

# Node
node_modules/

# Next.js build
.next/
out/

# Bun
*.tsbuildinfo

# Turbo
.turbo/
**/.turbo/

# Runtime logs
logs/
**/logs/

# Base de datos SQLite — nunca al repo
data/*.db
data/*.db-shm
data/*.db-wal
data/uploads/
apps/web/data/*.db
apps/web/data/*.db-shm
apps/web/data/*.db-wal

# Playwright — resultados de tests y reportes locales
test-results/
**/test-results/
playwright-report/
**/playwright-report/

# MCP browser session artifacts
.playwright-mcp/

# Brainstorming artifacts (internal design sessions)
.superpowers/

# App runtime logs (covered by logs/ but adding specific path)
apps/web/logs/

# Config env files by environment
config/*.env.*

# Internal design specs
docs/superpowers/

# Process ID files
*.pid
*.server-info
*.server-stopped

# Screenshots (verification artifacts, not versioned)
*.png
!apps/web/public/**/*.png

# OS
.DS_Store
