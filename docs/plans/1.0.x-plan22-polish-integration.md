# Plan 22 — Polish & Integration

> **Branch:** 1.0.x
> **Prerequisito:** Plan 23 + Plan 24 completados
> **Sprint:** think -> plan -> execute -> review -> test -> ship
> **Intensity:** standard
> **Orden:** Plan 23 -> Plan 24 -> Plan 22 -> Plan 25 (Messaging)

---

## Contexto

Plans 23 (code hardening) y 24 (admin/collections) dejaron deuda tecnica:

- CollectionSelector creado pero no integrado en ChatInterface
- `clean()` retorna `any` en safe-action — agujero de type safety
- `window.confirm` sigue en AdminRoles y AdminUsers
- Componentes nuevos sin tests (ChatInputBar, ConfirmDialog, admin components)
- `defaults.ts` parcialmente sin uso
- seed.ts referencia "tecpia"

Este plan cierra todos los cabos sueltos antes de pasar a Plan 25 (Messaging).

---

## Scope

**Fase 1: Integrar CollectionSelector en ChatInterface**
  — Wiring del selector en el input bar, pasar collection_names al generate

**Fase 2: Fixes directos (clean(), window.confirm, defaults, seed)**
  — Type safety, ConfirmDialog en admin, cleanup de código muerto

**Fase 3: Tests de componentes nuevos**
  — ChatInputBar, ConfirmDialog, admin components, CollectionDetail

**Fase 4: Quality gates + docs**
  — tsc + tests completos + CHANGELOG + CLAUDE.md

**Fuera de scope (Plan 25):**
- Messaging system completo (WebSocket, canales, threads, reactions)

---

## Fases

### Fase 1: Integrar CollectionSelector en ChatInterface

**Archivos:**
- `apps/web/src/components/chat/ChatInterface.tsx`
- `apps/web/src/components/chat/ChatInputBar.tsx`
- `apps/web/src/app/(app)/chat/[id]/page.tsx`

**Cambios:**

1. **ChatInterface recibe `availableCollections` como prop:**
   - La page RSC pasa las colecciones del usuario
   - ChatInterface mantiene estado `selectedCollections` via callback del CollectionSelector
   - Las `collection_names` se pasan al `DefaultChatTransport` body

2. **CollectionSelector en el input bar:**
   - Renderizar junto al label de coleccion existente en ChatInputBar
   - Reemplaza el `<span>` estático de `session.collection`
   - Solo se muestra si hay >1 coleccion disponible

3. **Wire al generate endpoint:**
   - `collection_names` ya existe en GenerateBodySchema (Plan 24 F1)
   - Solo falta pasarlo desde el transport body

**Verificacion:**
- [ ] Selector visible cuando user tiene >1 coleccion
- [ ] Cambio de seleccion se refleja en queries
- [ ] Single collection: no muestra selector, muestra label como antes

**Commit:** `feat(chat): wire CollectionSelector into ChatInterface — plan22 f1`

---

### Fase 2: Fixes directos

**Archivos:**
- `apps/web/src/lib/safe-action.ts` — fix `clean()` type
- `apps/web/src/components/admin/AdminRoles.tsx` — ConfirmDialog
- `apps/web/src/components/admin/AdminUsers.tsx` — ConfirmDialog
- `apps/web/src/lib/defaults.ts` — cleanup
- `packages/db/src/seed.ts` — kill "tecpia"

**Cambios:**

1. **Fix `clean()` return type:**
   - Cambiar de `any` a un mapped type que stripea undefined
   - O documentar con `@ts-expect-error` y comentario explicativo
   - Evaluar si vale la pena la complejidad vs el pragma

2. **ConfirmDialog en AdminRoles + AdminUsers:**
   - Reemplazar `window.confirm` con `ConfirmDialog` (mismo patron que SessionList)
   - AdminRoles: delete role confirmation
   - AdminUsers: delete user confirmation

3. **Cleanup defaults.ts:**
   - `DEFAULT_COLLECTION` ya no se usa en NavRail ni SessionList (usan props)
   - Mantener como fallback documentado o eliminar si ya no tiene callers

4. **Fix seed.ts:**
   - Reemplazar "tecpia" por "default" o configurable
   - Actualizar mensaje de log

**Verificacion:**
- [ ] Zero `window.confirm` en codebase activo
- [ ] `clean()` documentado/tipado
- [ ] Grep `DEFAULT_COLLECTION` confirma estado
- [ ] seed.ts no referencia "tecpia"

**Commit:** `fix(web): clean() type, ConfirmDialog in admin, cleanup defaults and seed — plan22 f2`

---

### Fase 3: Tests de componentes nuevos

**Archivos nuevos:**
- `apps/web/src/components/chat/__tests__/ChatInputBar.test.tsx`
- `apps/web/src/components/ui/__tests__/ConfirmDialog.test.tsx`
- `apps/web/src/components/admin/__tests__/AdminAreas.test.tsx`
- `apps/web/src/components/admin/__tests__/AdminCollections.test.tsx`
- `apps/web/src/components/admin/__tests__/AdminRagConfig.test.tsx`
- `apps/web/src/components/collections/__tests__/CollectionDetail.test.tsx`

**Test cases por componente:**

1. **ChatInputBar (~5):** render, send button disabled when empty, calls onSend,
   shows stop button when streaming, disabled state

2. **ConfirmDialog (~4):** renders title/description, calls onConfirm on confirm click,
   closes on cancel, controlled open/close

3. **AdminAreas (~4):** renders areas list, create form, delete with confirm,
   expand shows members

4. **AdminCollections (~3):** renders collections, create form, search filter

5. **AdminRagConfig (~3):** renders sliders, values change, save button state

6. **CollectionDetail (~3):** renders collection name, shows history table,
   shows chat button

**Verificacion:**
- [ ] `bun run test:components` -> all pass
- [ ] ~22 tests nuevos

**Commit:** `test(web): component tests for ChatInputBar, ConfirmDialog, admin, collections — plan22 f3`

---

### Fase 4: Quality gates + docs

- [ ] `bunx tsc --noEmit` -> 0 errors
- [ ] `bun run test` -> all pass
- [ ] `bun run test:components` -> all pass
- [ ] `grep "window.confirm"` en src/ -> 0 resultados
- [ ] `grep "tecpia"` en src/ -> 0 resultados (solo archive)
- [ ] CHANGELOG.md actualizado
- [ ] CLAUDE.md actualizado

**Commit:** `docs: changelog and claude.md update — plan22 f4`

---

## Dependencias nuevas

Ninguna.

## Metricas de exito

- [ ] CollectionSelector integrado y funcional en chat
- [ ] Zero `window.confirm` en codebase activo
- [ ] Zero "tecpia" fuera del archive
- [ ] ~22 component tests nuevos
- [ ] Codebase limpio para arrancar Plan 25 (Messaging)
