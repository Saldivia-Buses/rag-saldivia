# Plan 20 — BI & Analytics: Reporting, Dashboards, Exportacion

> **Prerequisito:** Plan 17 (ERP base) + Plan 18 (workflows) completados
> **Fecha:** 2026-04-10
> **Estado:** Draft — pendiente aprobacion
> **Version target:** 2.0.9
> **Sprint:** think > plan > execute > review > test > ship (por fase)
> **Servicios afectados:** `services/erp/`, `apps/web/`, `pkg/export/`
> **Estimacion:** 4 fases, ~5-7 dias
> **Migrations:** ninguna (solo queries + endpoints nuevos)

## Contexto

Histrix tiene ~169 pantallas de estadisticas/reportes repartidas en todos los
modulos. Son queries SQL hardcodeadas en XMLs que renderizan tablas HTML. No
hay graficos, no hay export, no hay filtros dinamicos.

SDA necesita un sistema de analytics que:
- Exponga KPIs por dominio via API
- Permita filtrar por periodo, entidad, centro de costo, etc.
- Tenga graficos en el frontend (evolucion, comparativo, ranking)
- Exporte a CSV/Excel
- Sea extensible (agregar un reporte nuevo = agregar un query + endpoint)

## Principios

1. **Reporting es read-only.** Cero writes, cero mutations, cero side effects.
2. **Las queries estan en sqlc.** Type-safe, auditables, performantes.
3. **Cada endpoint devuelve data + metadata.** El frontend decide como graficar.
4. **Export es un format parameter, no un endpoint separado.** `?format=csv`.

---

## Arquitectura

```
services/erp/
  internal/
    handler/
      analytics.go         ← HTTP handlers para todos los reportes
    service/
      analytics.go         ← Logica de aggregation + formatting
  db/
    queries/
      analytics_production.sql
      analytics_sales.sql
      analytics_purchasing.sql
      analytics_hr.sql
      analytics_quality.sql
      analytics_accounting.sql
      analytics_treasury.sql
      analytics_stock.sql

pkg/export/                ← NUEVO: exportacion CSV/Excel reutilizable
  csv.go                   ← CSV writer generico
  excel.go                 ← Excel writer (excelize)
  types.go                 ← ExportableData interface
```

### go.work

Agregar `pkg/export` al workspace:
```
// go.work — agregar linea:
use ./pkg/export
```

Y crear `pkg/export/go.mod` con dependency en `github.com/xuri/excelize/v2`.

### Routing y permisos

```go
r.Mount("/v1/erp/analytics", analyticsH.Routes())
```

No hay permiso especial de analytics. Cada sub-grupo de endpoints usa el
permiso de lectura del dominio correspondiente. Implementado con sub-routers:

```go
func (h *Analytics) Routes() chi.Router {
    r := chi.NewRouter()

    // Financieros: requieren erp.accounting.read
    r.Group(func(r chi.Router) {
        r.Use(sdamw.RequirePermission("erp.accounting.read"))
        r.Get("/accounting/balance-evolution", h.BalanceEvolution)
        r.Get("/accounting/income-expense", h.IncomeExpense)
        r.Get("/accounting/cost-center-breakdown", h.CostCenterBreakdown)
    })

    // Treasury: requieren erp.treasury.read
    r.Group(func(r chi.Router) {
        r.Use(sdamw.RequirePermission("erp.treasury.read"))
        r.Get("/treasury/cash-flow", h.CashFlow)
        r.Get("/treasury/balance-evolution", h.TreasuryBalanceEvolution)
        r.Get("/treasury/payment-methods", h.PaymentMethods)
    })

    // Invoicing: requieren erp.invoicing.read
    r.Group(func(r chi.Router) {
        r.Use(sdamw.RequirePermission("erp.invoicing.read"))
        r.Get("/invoicing/monthly-totals", h.MonthlyTotals)
        r.Get("/invoicing/tax-summary", h.TaxSummary)
        r.Get("/invoicing/top-customers", h.TopCustomers)
        r.Get("/invoicing/top-suppliers", h.TopSuppliers)
    })

    // ... mismo patron para cada dominio

    // Dashboard KPIs: requiere erp.catalogs.read (minimo — todos tienen esto)
    r.Group(func(r chi.Router) {
        r.Use(sdamw.RequirePermission("erp.catalogs.read"))
        r.Get("/dashboard/kpis", h.DashboardKPIs)
    })

    return r
}
```

Si podes ver contabilidad, podes ver reportes de contabilidad. Si solo tenes
permisos de stock, solo ves reportes de stock. El dashboard KPIs muestra solo
los KPIs para los cuales el usuario tiene permiso (los demas se omiten).

---

## Fase 0 — pkg/export + infraestructura de analytics

**Objetivo:** Package de exportacion reutilizable y handler base para analytics.

### pkg/export/

```go
package export

import (
    "encoding/csv"
    "io"
    "github.com/xuri/excelize/v2"
)

// Column define una columna exportable.
type Column struct {
    Header string
    Key    string
    Format string  // "text", "number", "currency", "date", "percent"
}

// Row es un map generico de datos.
type Row map[string]any

// WriteCSV escribe rows como CSV al writer.
func WriteCSV(w io.Writer, columns []Column, rows []Row) error {
    cw := csv.NewWriter(w)
    defer cw.Flush()

    // Header
    headers := make([]string, len(columns))
    for i, c := range columns { headers[i] = c.Header }
    cw.Write(headers)

    // Data
    for _, row := range rows {
        record := make([]string, len(columns))
        for i, c := range columns {
            record[i] = formatValue(row[c.Key], c.Format)
        }
        cw.Write(record)
    }
    return nil
}

// WriteExcel escribe rows como XLSX.
func WriteExcel(w io.Writer, sheetName string, columns []Column, rows []Row) error {
    f := excelize.NewFile()
    // ... header row with bold style
    // ... data rows with number formatting
    // ... auto-width columns
    return f.Write(w)
}
```

### Handler base pattern

```go
// analytics.go — pattern comun para todos los reportes
func (h *Analytics) handleReport(w http.ResponseWriter, r *http.Request,
    columns []export.Column, fetchFn func() ([]export.Row, error)) {

    rows, err := fetchFn()
    if err != nil { ... }

    format := r.URL.Query().Get("format")
    switch format {
    case "csv":
        w.Header().Set("Content-Type", "text/csv")
        w.Header().Set("Content-Disposition", "attachment; filename=report.csv")
        export.WriteCSV(w, columns, rows)
    case "excel":
        w.Header().Set("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        w.Header().Set("Content-Disposition", "attachment; filename=report.xlsx")
        export.WriteExcel(w, "Reporte", columns, rows)
    default:
        // JSON (default)
        json.NewEncoder(w).Encode(map[string]any{
            "columns": columns,
            "rows":    rows,
            "meta":    map[string]any{"count": len(rows)},
        })
    }
}
```

### Frontend: chart library

Agregar `recharts` al frontend. Componentes reutilizables:

```
apps/web/src/components/analytics/
  BarChart.tsx             ← Barras (comparativos)
  LineChart.tsx            ← Lineas (evolucion temporal)
  PieChart.tsx             ← Torta (composicion)
  KPICard.tsx              ← Tarjeta con valor + trend
  ReportTable.tsx          ← Tabla con sort + export buttons
  DateRangeFilter.tsx      ← Filtro de periodo reutilizable
  ExportButtons.tsx        ← Botones CSV/Excel
```

---

## Fase 1 — Reportes financieros (contabilidad + tesoreria + facturacion)

### Endpoints

| Method | Path | Descripcion |
|---|---|---|
| GET | `/v1/erp/analytics/accounting/balance-evolution` | Evolucion de balance por mes |
| GET | `/v1/erp/analytics/accounting/income-expense` | Resultado: ingresos vs egresos por periodo |
| GET | `/v1/erp/analytics/accounting/cost-center-breakdown` | Gastos por centro de costo |
| GET | `/v1/erp/analytics/treasury/cash-flow` | Flujo de caja (ingresos vs egresos por mes) |
| GET | `/v1/erp/analytics/treasury/balance-evolution` | Evolucion saldos bancarios |
| GET | `/v1/erp/analytics/treasury/payment-methods` | Distribucion por medio de pago |
| GET | `/v1/erp/analytics/invoicing/monthly-totals` | Facturacion mensual (emitida vs recibida) |
| GET | `/v1/erp/analytics/invoicing/tax-summary` | Resumen IVA por periodo |
| GET | `/v1/erp/analytics/invoicing/top-customers` | Ranking clientes por facturacion |
| GET | `/v1/erp/analytics/invoicing/top-suppliers` | Ranking proveedores por compras |
| GET | `/v1/erp/analytics/accounts/aging` | Aging de cuentas por cobrar/pagar |
| GET | `/v1/erp/analytics/accounts/collection-rate` | Tasa de cobranza por periodo |

### Queries de ejemplo

```sql
-- name: AnalyticsIncomeExpense :many
-- Resultado mensual: ingresos vs egresos
SELECT
    to_char(je.date, 'YYYY-MM') AS period,
    SUM(CASE WHEN a.account_type = 'income' THEN jl.credit - jl.debit ELSE 0 END) AS income,
    SUM(CASE WHEN a.account_type = 'expense' THEN jl.debit - jl.credit ELSE 0 END) AS expense
FROM erp_journal_entries je
JOIN erp_journal_lines jl ON jl.entry_id = je.id AND jl.tenant_id = je.tenant_id
JOIN erp_accounts a ON a.id = jl.account_id
WHERE je.tenant_id = @tenant_id
    AND je.status = 'posted'
    AND je.date BETWEEN @date_from AND @date_to
GROUP BY to_char(je.date, 'YYYY-MM')
ORDER BY period;

-- name: AnalyticsCashFlow :many
-- Flujo de caja mensual
SELECT
    to_char(date, 'YYYY-MM') AS period,
    SUM(CASE WHEN movement_type IN ('cash_in', 'bank_deposit', 'check_received') THEN amount ELSE 0 END) AS inflow,
    SUM(CASE WHEN movement_type IN ('cash_out', 'bank_withdrawal', 'check_issued') THEN amount ELSE 0 END) AS outflow
FROM erp_treasury_movements
WHERE tenant_id = @tenant_id
    AND status = 'confirmed'
    AND date BETWEEN @date_from AND @date_to
GROUP BY to_char(date, 'YYYY-MM')
ORDER BY period;

-- name: AnalyticsAging :many
-- Aging de cuentas por cobrar
SELECT
    e.id AS entity_id, e.name AS entity_name,
    SUM(CASE WHEN am.balance > 0 AND i.due_date >= CURRENT_DATE THEN am.balance ELSE 0 END) AS current,
    SUM(CASE WHEN am.balance > 0 AND i.due_date BETWEEN CURRENT_DATE - 30 AND CURRENT_DATE - 1 THEN am.balance ELSE 0 END) AS days_1_30,
    SUM(CASE WHEN am.balance > 0 AND i.due_date BETWEEN CURRENT_DATE - 60 AND CURRENT_DATE - 31 THEN am.balance ELSE 0 END) AS days_31_60,
    SUM(CASE WHEN am.balance > 0 AND i.due_date BETWEEN CURRENT_DATE - 90 AND CURRENT_DATE - 61 THEN am.balance ELSE 0 END) AS days_61_90,
    SUM(CASE WHEN am.balance > 0 AND i.due_date < CURRENT_DATE - 90 THEN am.balance ELSE 0 END) AS days_over_90
FROM erp_account_movements am
JOIN erp_entities e ON e.id = am.entity_id
LEFT JOIN erp_invoices i ON i.id = am.invoice_id
WHERE am.tenant_id = @tenant_id
    AND am.direction = @direction  -- 'receivable' o 'payable'
    AND am.balance > 0
GROUP BY e.id, e.name
ORDER BY SUM(am.balance) DESC;
```

### Frontend

Nueva pagina `estadisticas/page.tsx` con tabs por dominio:

Tab "Financiero":
- KPI cards: saldo total, facturado mes, cobrado mes, pendiente cobro
- LineChart: evolucion ingresos vs egresos (12 meses)
- BarChart: flujo de caja mensual
- PieChart: distribucion por medio de pago
- Table: aging de cuentas por cobrar

---

## Fase 2 — Reportes operativos (produccion + compras + ventas + stock)

### Endpoints

| Method | Path | Descripcion |
|---|---|---|
| GET | `/v1/erp/analytics/production/orders-by-status` | OPs por estado (pipeline) |
| GET | `/v1/erp/analytics/production/output-by-month` | Produccion mensual (unidades) |
| GET | `/v1/erp/analytics/production/efficiency` | Eficiencia: planned vs actual time |
| GET | `/v1/erp/analytics/production/material-consumption` | Consumo de materiales vs BOM |
| GET | `/v1/erp/analytics/purchasing/orders-by-month` | Compras mensuales |
| GET | `/v1/erp/analytics/purchasing/supplier-ranking` | Ranking proveedores (volumen + demerits) |
| GET | `/v1/erp/analytics/purchasing/delivery-performance` | Cumplimiento de entrega |
| GET | `/v1/erp/analytics/sales/quotation-conversion` | Tasa conversion cotizacion → pedido |
| GET | `/v1/erp/analytics/sales/orders-by-month` | Ventas mensuales |
| GET | `/v1/erp/analytics/sales/customer-concentration` | Concentracion de ventas por cliente |
| GET | `/v1/erp/analytics/stock/valuation` | Valuacion de inventario |
| GET | `/v1/erp/analytics/stock/rotation` | Rotacion de stock (ABC) |
| GET | `/v1/erp/analytics/stock/below-minimum` | Articulos debajo del minimo |
| GET | `/v1/erp/analytics/stock/movement-summary` | Resumen movimientos por tipo |

### Queries de ejemplo

```sql
-- name: AnalyticsStockRotation :many
-- Rotacion de stock (ultimos N meses)
-- Rotation = consumed / average_stock (not current_stock, which is a point-in-time snapshot).
-- Average stock approximated as (current_stock + consumed_qty) / 2 (beginning ≈ current + consumed).
SELECT
    a.id, a.code, a.name,
    COALESCE(sl.quantity, 0) AS current_stock,
    COALESCE(a.last_cost, 0) AS unit_cost,
    COALESCE(sl.quantity, 0) * COALESCE(a.last_cost, 0) AS stock_value,
    COALESCE(consumed.qty, 0) AS consumed_qty,
    CASE WHEN (COALESCE(sl.quantity, 0) + COALESCE(consumed.qty, 0)) > 0
        THEN COALESCE(consumed.qty, 0) / ((COALESCE(sl.quantity, 0) + COALESCE(consumed.qty, 0)) / 2.0)
        ELSE 0
    END AS rotation_index
FROM erp_articles a
LEFT JOIN erp_stock_levels sl ON sl.article_id = a.id AND sl.tenant_id = a.tenant_id
LEFT JOIN (
    SELECT article_id, SUM(ABS(quantity)) AS qty
    FROM erp_stock_movements
    WHERE tenant_id = @tenant_id AND movement_type = 'out'
        AND created_at >= @date_from
    GROUP BY article_id
) consumed ON consumed.article_id = a.id
WHERE a.tenant_id = @tenant_id AND a.active = true
ORDER BY rotation_index DESC;

-- name: AnalyticsProductionEfficiency :many
-- NOTE: end_date and start_date are DATE type. DATE - DATE in PostgreSQL
-- returns INTEGER (number of days) directly — no EXTRACT/EPOCH needed.
SELECT
    pc.name AS center_name,
    COUNT(*) AS total_orders,
    COUNT(*) FILTER (WHERE po.status = 'completed') AS completed,
    AVG(po.end_date - po.start_date)
        FILTER (WHERE po.status = 'completed' AND po.end_date IS NOT NULL AND po.start_date IS NOT NULL)
        AS avg_days
FROM erp_production_orders po
LEFT JOIN erp_production_centers pc ON pc.id = po.center_id
WHERE po.tenant_id = @tenant_id
    AND po.date BETWEEN @date_from AND @date_to
GROUP BY pc.name
ORDER BY total_orders DESC;
```

### Frontend

Tabs adicionales en `estadisticas/page.tsx`:

Tab "Produccion":
- KPI cards: OPs activas, completadas este mes, eficiencia promedio
- BarChart: pipeline de OPs por estado
- LineChart: produccion mensual
- Table: eficiencia por centro productivo

Tab "Compras":
- KPI cards: total compras mes, OC pendientes, entregas atrasadas
- BarChart: compras mensuales
- Table: ranking proveedores con demerits

Tab "Ventas":
- KPI cards: ventas mes, cotizaciones pendientes, tasa conversion
- LineChart: ventas mensuales
- PieChart: concentracion por cliente
- Table: cotizaciones con estado

Tab "Stock":
- KPI cards: valor inventario, items bajo minimo, items sin movimiento
- Table: rotacion ABC (exportable)
- BarChart: movimientos por tipo

---

## Fase 3 — Reportes RRHH + calidad + dashboards

### Endpoints

| Method | Path | Descripcion |
|---|---|---|
| GET | `/v1/erp/analytics/hr/headcount` | Headcount por departamento/seccion |
| GET | `/v1/erp/analytics/hr/absences-by-month` | Ausentismo mensual |
| GET | `/v1/erp/analytics/hr/overtime-summary` | Horas extra por periodo |
| GET | `/v1/erp/analytics/hr/seniority` | Distribucion por antiguedad |
| GET | `/v1/erp/analytics/quality/nonconformities-by-type` | NC por tipo/area |
| GET | `/v1/erp/analytics/quality/resolution-time` | Tiempo promedio de resolucion |
| GET | `/v1/erp/analytics/quality/audit-results` | Resultados de auditorias |
| GET | `/v1/erp/analytics/maintenance/completion-rate` | Tasa de completitud WO |
| GET | `/v1/erp/analytics/maintenance/downtime-by-asset` | Tiempo caido por equipo |
| GET | `/v1/erp/analytics/maintenance/cost-by-asset` | Costo mantenimiento por equipo |
| GET | `/v1/erp/analytics/dashboard/kpis` | KPIs consolidados (todos los dominios) |

### Dashboard KPIs (endpoint consolidado)

```go
type DashboardKPIs struct {
    // Financiero
    MonthRevenue      float64 `json:"month_revenue"`
    MonthExpense      float64 `json:"month_expense"`
    CashBalance       float64 `json:"cash_balance"`
    AccountsReceivable float64 `json:"accounts_receivable"`
    AccountsPayable   float64 `json:"accounts_payable"`

    // Operativo
    ActiveProdOrders  int     `json:"active_prod_orders"`
    PendingPurchases  int     `json:"pending_purchases"`
    OpenQuotations    int     `json:"open_quotations"`
    StockBelowMin     int     `json:"stock_below_min"`

    // RRHH
    Headcount         int     `json:"headcount"`
    AbsencesThisMonth int     `json:"absences_this_month"`

    // Calidad
    OpenNonconformities int   `json:"open_nonconformities"`
    PendingWorkOrders   int   `json:"pending_work_orders"`
}
```

Este endpoint ejecuta ~12 queries simples (COUNT/SUM) en paralelo usando
goroutines. Cada query es rapida por si sola; paralelizarlas hace que el
endpoint responda en <200ms.

### Frontend

Tab "Dashboard" (landing page de estadisticas):
- Grid de KPI cards con trend arrows
- Mini-charts inline (sparklines)
- Links a cada reporte detallado

Tabs adicionales:
- "RRHH": headcount por area, ausentismo, horas extra
- "Calidad": NC por tipo, tiempos de resolucion
- "Mantenimiento": completitud WO, costo por equipo

---

## Resumen

| Fase | Scope | Endpoints | Frontend |
|---|---|---|---|
| 0 — Infra | pkg/export + handler pattern + charts | 0 | Componentes base |
| 1 — Financieros | Contabilidad + tesoreria + facturacion + ctas | 12 | Tab Financiero |
| 2 — Operativos | Produccion + compras + ventas + stock | 14 | 4 tabs |
| 3 — RRHH/Calidad/Dashboard | RRHH + calidad + mantenimiento + KPIs | 11 | 4 tabs |
| **Total** | | **37 endpoints** | **9 tabs** |

## Post-plan: que queda reutilizable

- **pkg/export/** — CSV/Excel export generico. Reutilizable por cualquier
  servicio que necesite exportar data (search results, audit logs, etc.)
- **Componentes analytics frontend** — charts, KPI cards, filtros, export
  buttons. Reutilizables en cualquier pagina del sistema.
- **Pattern: query + format param** — patron simple para agregar reportes sin
  crear endpoints especiales de export.

## Consideraciones de performance

- Todas las queries de analytics usan los indices ya existentes de plan 17.
- Para datasets grandes (>100k rows), el endpoint devuelve datos paginados
  y el export genera el archivo en background con descarga async.
- No se crean tablas de aggregation/materialized views en V1. Si la performance
  de alguna query se degrada con volumen, se agrega una materialized view
  como optimizacion puntual.
