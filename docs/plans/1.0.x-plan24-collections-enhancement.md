# Plan 24 — Admin System: Collections, Areas, Permissions, Config

> **Estado:** COMPLETADO — 2026-03-30
> **Branch:** 1.0.x
> **Líneas originales:** ~485 → comprimido a resumen post-ejecución

---

## Qué se hizo

Completar el admin de 3/7 secciones a 7/7. Arreglar bugs de colecciones. Conectar el flujo Users → Areas → Collections → Permissions.

### Estado antes → después

**Antes (3/7):** Dashboard, Users, Roles
**Después (7/7):** Dashboard, Users, Roles, **Areas**, **Permissions**, **Collections**, **Config**

### Bugs de colecciones corregidos

- DELETE no invalidaba cache Redis (60s stale)
- POST devolvía `apiOk()` cuando RAG server fallaba
- Auth inconsistente entre rutas
- `"tecpia"` hardcoded en 3 lugares → `DEFAULT_COLLECTION` configurable via env
- `RagGenerateRequest` sin `collection_names` en el tipo
- Sin validación Zod de nombres de colecciones

### Código recuperado del archive

- `AreasAdmin.tsx` — CRUD áreas con table view
- `PermissionsAdmin.tsx` — matriz área-colección read/write
- `RagConfigAdmin.tsx` — sliders para params RAG
- `CollectionSelector.tsx` — multi-collection selector para chat
- Server actions de áreas

### Páginas nuevas

```
/admin/areas        → CRUD áreas con miembros y colecciones
/admin/permissions  → matriz area-colección read/write/admin
/admin/collections  → gestión completa de colecciones
/admin/config       → parámetros RAG (temperature, top_k, etc.)
```

### Commits

| Fase | Commit | Descripción |
|------|--------|-------------|
| Todo | `88c8901` | feat(admin): collections, areas, permissions, config, user prefs |
