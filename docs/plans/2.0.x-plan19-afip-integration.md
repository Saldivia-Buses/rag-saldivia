# Plan 19 — AFIP Integration: Facturacion Electronica & Compliance Impositivo

> **Prerequisito:** Plan 18 fase 2 (anulacion cascada) + plan 17 facturacion funcionando
> **Fecha:** 2026-04-10
> **Estado:** Draft — pendiente aprobacion
> **Version target:** 2.0.8
> **Sprint:** think > plan > execute > review > test > ship (por fase)
> **Servicios afectados:** `services/erp/`, `apps/web/`, `pkg/afip/`, `db/tenant/migrations/`
> **Estimacion:** 4 fases, ~8-10 dias
> **Migrations:** 035-038

## Contexto

Toda empresa argentina que factura necesita autorizar sus comprobantes ante
AFIP (Administracion Federal de Ingresos Publicos) via webservice. Histrix
tiene esto implementado en PHP con SOAP directo. SDA necesita su propia
integracion para:

1. **Emitir facturas electronicas** con CAE (Codigo de Autorizacion Electronico)
2. **Generar el Libro IVA Digital** en formato AFIP (CITI)
3. **Exportar retenciones en formato SIFERE** (Sistema Federal de Recaudacion)
4. **Consultar el padron de contribuyentes** (validar CUIT, condicion IVA)

**Sin AFIP, las facturas emitidas no tienen validez fiscal.**

## Principios

1. **AFIP es un sistema externo inestable.** Reintentos, timeouts, circuit breaker.
2. **Separar protocolo de logica.** `pkg/afip/` maneja SOAP/WSDL, `services/erp/` maneja logica.
3. **Ambiente de homologacion primero.** Todo se desarrolla contra el sandbox de AFIP.
4. **Certificados por tenant.** Cada empresa tiene su propio certificado digital AFIP.

---

## Arquitectura

```
services/erp/
  internal/
    handler/
      afip.go              ← HTTP handlers AFIP-specific
    service/
      afip.go              ← Logica de facturacion electronica, libro IVA, SIFERE
      afip_cae.go          ← Workflow de solicitud de CAE
  ...

pkg/afip/                  ← NUEVO: client AFIP reutilizable
  wsfe.go                  ← Client WSFE (factura electronica)
  wsaa.go                  ← Client WSAA (autenticacion)
  padron.go                ← Client padron A5/A10/A13
  types.go                 ← Tipos compartidos (FECAERequest, etc.)
  xml.go                   ← Marshaling XML SOAP
  errors.go                ← Error codes AFIP → mensajes legibles
  client.go                ← HTTP client con retry + circuit breaker
  testdata/                ← Fixtures para tests
```

### SOAP approach

AFIP usa SOAP/XML exclusivamente. Go no tiene soporte nativo de SOAP. Approach:

- **Manual XML marshaling con `encoding/xml`** (recomendado). Los WSDLs de AFIP
  son non-standard y rompen los code generators. Es mas confiable escribir los
  structs manualmente con tags `xml:` y hacer HTTP POST con el envelope construido.
- `xml.go` implementa un helper para construir SOAP envelopes genericos.
- NO usar `gowsdl` ni otros code generators — AFIP devuelve XML malformado en
  algunos casos edge que necesitan handling manual.

### go.work

Agregar `pkg/afip` al workspace:
```
// go.work — agregar linea:
use ./pkg/afip
```

Y crear `pkg/afip/go.mod` con las dependencias del package.

### Flujo de autenticacion AFIP (WSAA)

```
1. ERP tiene certificado digital (.crt) + clave privada (.key) por tenant
2. Firma un TRA (Ticket de Requerimiento de Acceso) con la clave privada
3. Envia el TRA firmado al WSAA
4. Recibe un TA (Ticket de Acceso) con token + sign (valido 12hs)
5. Usa token + sign en cada request a WSFE, padron, etc.
```

Los TAs se cachean en Redis (TTL = expiration del TA - 5 minutos).

---

## Fase 0 — pkg/afip + WSAA (autenticacion)

**Objetivo:** Package reutilizable que autentica contra AFIP y obtiene tickets.

### pkg/afip/wsaa.go

```go
package afip

type WSAAClient struct {
    endpoint    string          // homologacion o produccion
    httpClient  *http.Client
    cache       *redis.Client   // para cachear TAs
}

type TicketAccess struct {
    Token      string
    Sign       string
    ExpiresAt  time.Time
}

// Authenticate obtiene un TA para el servicio indicado.
// Si hay uno valido en cache, lo retorna sin ir a AFIP.
func (c *WSAAClient) Authenticate(ctx context.Context, tenantID string, service string, cert []byte, key []byte) (*TicketAccess, error)
```

### Schema (certificados AFIP por tenant)

```sql
-- 035_erp_afip_config.up.sql

-- Configuracion AFIP por tenant
CREATE TABLE erp_afip_config (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       TEXT NOT NULL UNIQUE,
    cuit            TEXT NOT NULL,           -- CUIT del emisor
    environment     TEXT NOT NULL DEFAULT 'homologacion',  -- 'homologacion', 'produccion'
    certificate     BYTEA NOT NULL,         -- .crt (encrypted con pkg/crypto)
    private_key     BYTEA NOT NULL,         -- .key (encrypted con pkg/crypto)
    sale_point      INT NOT NULL DEFAULT 1, -- punto de venta AFIP
    last_invoice_a  BIGINT NOT NULL DEFAULT 0,  -- ultimo nro autorizado
    last_invoice_b  BIGINT NOT NULL DEFAULT 0,
    last_invoice_c  BIGINT NOT NULL DEFAULT 0,
    last_credit_note_a BIGINT NOT NULL DEFAULT 0,
    last_credit_note_b BIGINT NOT NULL DEFAULT 0,
    last_credit_note_c BIGINT NOT NULL DEFAULT 0,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- RLS
ALTER TABLE erp_afip_config ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON erp_afip_config
    USING (tenant_id = current_setting('app.tenant_id', true));

-- Permisos
INSERT INTO permissions (id, name, description, category) VALUES
    ('erp.afip.read', 'Ver config AFIP', 'Consultar configuracion AFIP', 'erp'),
    ('erp.afip.write', 'Gestionar AFIP', 'Configurar certificados y punto de venta', 'erp'),
    ('erp.afip.emit', 'Emitir comprobantes AFIP', 'Solicitar CAE', 'erp')
ON CONFLICT (id) DO NOTHING;
```

### Endpoints

| Method | Path | Permiso | Descripcion |
|---|---|---|---|
| GET | `/v1/erp/afip/config` | `erp.afip.read` | Config actual del tenant |
| PUT | `/v1/erp/afip/config` | `erp.afip.write` | Subir certificado + key + config |
| POST | `/v1/erp/afip/test-auth` | `erp.afip.write` | Test de autenticacion contra WSAA |

### Tests

```go
func TestWSAAAuthenticate(t *testing.T) {
    // Usa certificado de homologacion de AFIP (publico, para testing)
    cert, key := loadTestCert(t)
    ta, err := client.Authenticate(ctx, "test-tenant", "wsfe", cert, key)
    assert.NoError(t, err)
    assert.NotEmpty(t, ta.Token)
    assert.True(t, ta.ExpiresAt.After(time.Now()))
}
```

---

## Fase 1 — Facturacion electronica (WSFE + CAE)

**Objetivo:** Solicitar CAE a AFIP para cada factura emitida. El CAE es
obligatorio para que la factura tenga validez fiscal.

### Flujo

```
1. Usuario crea factura en SDA (status='draft')
2. Usuario clickea "Autorizar AFIP" (o se autoriza automaticamente al postear)
3. Service layer:
   a. Obtiene TA via WSAA (cache o nuevo)
   b. Construye FECAERequest con datos de la factura
   c. Llama a FECAESolicitar en WSFE
   d. Recibe CAE + fecha vencimiento
   e. Guarda CAE en erp_invoices (afip_cae, afip_cae_due)
   f. Genera QR con datos fiscales
   g. Marca factura como 'posted'
4. Si AFIP rechaza: guarda errores, factura queda en 'draft'
```

### pkg/afip/wsfe.go

```go
type WSFEClient struct {
    endpoint   string
    httpClient *http.Client
}

type FECAERequest struct {
    CbteTipo    int         // 1=FA, 2=NDA, 3=NCA, 6=FB, etc.
    PtoVta      int
    CbteDesde   int64
    CbteHasta   int64
    CbteFch     string      // YYYYMMDD
    ImpTotal    float64
    ImpNeto     float64
    ImpIVA      float64
    MonId       string      // "PES", "DOL"
    DocTipo     int         // 80=CUIT, 96=DNI
    DocNro      int64
    IVA         []AlicuotaIVA
}

type FECAEResponse struct {
    CAE         string
    CAEFchVto   string      // YYYYMMDD
    Resultado   string      // "A"=aprobado, "R"=rechazado
    Errores     []AFIPError
    Observaciones []AFIPObs
}

func (c *WSFEClient) SolicitarCAE(ctx context.Context, ta *TicketAccess, req *FECAERequest) (*FECAEResponse, error)
func (c *WSFEClient) ConsultarUltimoAutorizado(ctx context.Context, ta *TicketAccess, ptoVta int, cbteTipo int) (int64, error)
func (c *WSFEClient) ConsultarComprobante(ctx context.Context, ta *TicketAccess, ptoVta int, cbteTipo int, cbteNro int64) (*Comprobante, error)
```

### Schema

```sql
-- 036_erp_afip_log.up.sql

-- Log de requests/responses AFIP (para auditoria y debugging)
CREATE TABLE erp_afip_log (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       TEXT NOT NULL,
    invoice_id      UUID REFERENCES erp_invoices(id),
    operation       TEXT NOT NULL,           -- 'FECAESolicitar', 'FECompConsultar'
    request_xml     TEXT NOT NULL,
    response_xml    TEXT,
    cae             TEXT,
    resultado       TEXT,                    -- 'A', 'R'
    errors          JSONB DEFAULT '[]',
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_erp_afip_log_invoice ON erp_afip_log(tenant_id, invoice_id);

-- RLS
ALTER TABLE erp_afip_log ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON erp_afip_log
    USING (tenant_id = current_setting('app.tenant_id', true));
```

### Endpoints

| Method | Path | Permiso | Descripcion |
|---|---|---|---|
| POST | `/v1/erp/invoicing/invoices/{id}/authorize` | `erp.afip.emit` | Solicitar CAE a AFIP |
| GET | `/v1/erp/invoicing/invoices/{id}/afip-status` | `erp.afip.read` | Estado AFIP + log |
| POST | `/v1/erp/afip/sync-last` | `erp.afip.write` | Sincronizar ultimo nro autorizado |
| GET | `/v1/erp/invoicing/invoices/{id}/qr` | `erp.invoicing.read` | QR code fiscal (PNG) |

### QR Fiscal

AFIP requiere un QR en cada factura con datos codificados en base64:

```go
type QRData struct {
    Ver     int    `json:"ver"`     // 1
    Fecha   string `json:"fecha"`   // "2026-04-10"
    CUIT    int64  `json:"cuit"`
    PtoVta  int    `json:"ptoVta"`
    TipoCmp int    `json:"tipoCmp"`
    NroCmp  int64  `json:"nroCmp"`
    Importe float64 `json:"importe"`
    Moneda  string `json:"moneda"`
    Ctz     float64 `json:"ctz"`
    TipoDocRec int  `json:"tipoDocRec"`
    NroDocRec  int64 `json:"nroDocRec"`
    TipoCodAut string `json:"tipoCodAut"` // "E"
    CodAut     int64  `json:"codAut"`     // CAE
}
// Se serializa a JSON, base64, y se codifica en QR apuntando a:
// https://www.afip.gob.ar/fe/qr/?p={base64}
```

### Service layer

```go
func (s *AFIP) AuthorizeInvoice(ctx context.Context, slug string, invoiceID uuid.UUID) (*AuthorizeResult, error) {
    tx, _ := s.pool.BeginTx(ctx)
    defer tx.Rollback(ctx)

    inv := s.repo.GetInvoice(ctx, tx, slug, invoiceID)
    if inv.Status != "draft" { return nil, errors.New("solo draft se puede autorizar") }
    if inv.Direction != "issued" { return nil, errors.New("solo comprobantes emitidos") }

    config := s.repo.GetAFIPConfig(ctx, tx, slug)

    // Autenticar
    ta, err := s.wsaa.Authenticate(ctx, slug, "wsfe", config.Certificate, config.PrivateKey)
    if err != nil { return nil, fmt.Errorf("WSAA auth: %w", err) }

    // Construir request
    cbteTipo := mapInvoiceType(inv.InvoiceType)
    cbteNro := s.getNextNumber(config, cbteTipo)
    req := s.buildFECAERequest(inv, config, cbteTipo, cbteNro)

    // Solicitar CAE
    resp, err := s.wsfe.SolicitarCAE(ctx, ta, req)

    // Log SOAP (siempre, aprobado o rechazado)
    s.repo.CreateAFIPLog(ctx, tx, slug, invoiceID, "FECAESolicitar", reqXML, respXML, resp)

    if err != nil || resp.Resultado == "R" {
        tx.Commit(ctx)
        return &AuthorizeResult{Approved: false, Errors: resp.Errores}, nil
    }

    // Guardar CAE
    s.repo.SetInvoiceCAE(ctx, tx, slug, invoiceID, resp.CAE, resp.CAEFchVto)
    s.repo.UpdateLastNumber(ctx, tx, slug, cbteTipo, cbteNro)

    tx.Commit(ctx)
    s.audit.Strict(ctx, "afip_authorize", ...)
    return &AuthorizeResult{Approved: true, CAE: resp.CAE}, nil
}
```

### Frontend

Boton "Autorizar AFIP" en detalle de factura draft emitida. Muestra resultado
(aprobado con CAE, o rechazado con errores AFIP legibles). Badge verde
"Autorizada" con CAE visible. Boton para descargar/imprimir con QR.

---

## Fase 2 — Libro IVA Digital + SIFERE

**Objetivo:** Generar los archivos regulatorios que AFIP exige mensualmente.

### Libro IVA Digital (CITI)

AFIP requiere dos archivos por periodo (mes):
- **CITI Compras:** todas las facturas recibidas
- **CITI Ventas:** todas las facturas emitidas

Formato: archivo de texto con posiciones fijas (fixed-width columns).

### SIFERE (retenciones)

Formato para declarar retenciones practicadas/sufridas. Archivo de texto con
posiciones fijas.

### Schema

```sql
-- 037_erp_tax_exports.up.sql

-- Historial de exportaciones (para saber que ya se genero)
CREATE TABLE erp_tax_exports (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       TEXT NOT NULL,
    export_type     TEXT NOT NULL,           -- 'citi_compras', 'citi_ventas', 'sifere'
    period          TEXT NOT NULL,           -- '2026-04'
    file_key        TEXT NOT NULL,           -- MinIO key del archivo generado
    record_count    INT NOT NULL,
    total_amount    NUMERIC(16,2) NOT NULL,
    generated_by    TEXT NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_erp_tax_exports ON erp_tax_exports(tenant_id, period, export_type);

-- RLS
ALTER TABLE erp_tax_exports ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON erp_tax_exports
    USING (tenant_id = current_setting('app.tenant_id', true));
```

### Endpoints

| Method | Path | Permiso | Descripcion |
|---|---|---|---|
| POST | `/v1/erp/afip/citi/generate` | `erp.afip.emit` | Generar CITI compras+ventas (body: {period}) |
| GET | `/v1/erp/afip/citi/download/{id}` | `erp.afip.read` | Descargar archivo CITI |
| POST | `/v1/erp/afip/sifere/generate` | `erp.afip.emit` | Generar SIFERE (body: {period}) |
| GET | `/v1/erp/afip/sifere/download/{id}` | `erp.afip.read` | Descargar archivo SIFERE |
| GET | `/v1/erp/afip/exports` | `erp.afip.read` | Historial de exportaciones |

### CITI format (ejemplo compras)

```go
type CITIComprasLine struct {
    Fecha           string  // YYYYMMDD (8)
    Tipo            string  // codigo comprobante (3)
    PtoVenta        string  // 5 digits right-aligned
    NroComprobante  string  // 20 digits right-aligned
    ImporteDeOperaciones string // 15 digits + 2 decimals
    // ... (20+ campos mas)
}

func (s *AFIP) GenerateCITI(ctx context.Context, slug string, period string, direction string) (string, error) {
    invoices := s.repo.ListInvoicesForCITI(ctx, slug, period, direction)
    var buf bytes.Buffer
    for _, inv := range invoices {
        line := formatCITILine(inv)
        buf.WriteString(line + "\r\n")
    }
    key := fmt.Sprintf("afip/%s/citi_%s_%s.txt", slug, direction, period)
    s.storage.Upload(ctx, key, buf.Bytes())
    s.repo.CreateTaxExport(ctx, slug, ...)
    return key, nil
}
```

### Frontend

Nueva tab "AFIP" en `facturacion/page.tsx` con:
- Selector de periodo
- Botones "Generar CITI Compras", "Generar CITI Ventas", "Generar SIFERE"
- Tabla con historial de archivos generados y boton descargar
- Preview de totales antes de generar

---

## Fase 3 — Padron de contribuyentes

**Objetivo:** Consultar datos de un CUIT en el padron de AFIP. Autocompletar
datos al crear entidades (clientes/proveedores).

### Webservices de padron

AFIP tiene varios webservices de consulta:
- **A5 (ws_sr_padron_a5):** datos basicos (nombre, tipo doc, actividad)
- **A10 (ws_sr_padron_a10):** datos extendidos + domicilio fiscal
- **A13 (ws_sr_padron_a13):** condicion tributaria + categoria monotributo

### pkg/afip/padron.go

```go
type PadronClient struct {
    endpoint   string
    httpClient *http.Client
}

type PersonaReturn struct {
    CUIT            int64
    Nombre          string
    TipoPersona     string   // "FISICA", "JURIDICA"
    EstadoClave     string   // "ACTIVO", "INACTIVO"
    DomicilioFiscal *Domicilio
    Actividades     []Actividad
    Impuestos       []Impuesto
    Categorias      []Categoria
    MonoCategoria   string   // para monotributistas
}

func (c *PadronClient) GetPersona(ctx context.Context, ta *TicketAccess, cuit int64) (*PersonaReturn, error)
```

### Schema

```sql
-- 038_erp_padron_cache.up.sql

-- Cache local del padron (para no consultar AFIP cada vez)
CREATE TABLE erp_padron_cache (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    cuit        TEXT NOT NULL UNIQUE,
    data        JSONB NOT NULL,             -- PersonaReturn completo
    fetched_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_erp_padron_cuit ON erp_padron_cache(cuit);
-- No tiene tenant_id: el padron es publico, compartido entre tenants
-- No tiene RLS por la misma razon
```

### Endpoints

| Method | Path | Permiso | Descripcion |
|---|---|---|---|
| GET | `/v1/erp/afip/padron/{cuit}` | `erp.afip.read` | Consultar CUIT (cache 24hs → AFIP live) |

### Service layer

```go
func (s *AFIP) GetPadron(ctx context.Context, slug string, cuit string) (*PersonaReturn, error) {
    // 1. Buscar en cache (< 24hs)
    cached := s.repo.GetPadronCache(ctx, cuit)
    if cached != nil && cached.FetchedAt.After(time.Now().Add(-24*time.Hour)) {
        return cached.Data, nil
    }

    // 2. Consultar AFIP
    config := s.repo.GetAFIPConfig(ctx, slug)
    ta, err := s.wsaa.Authenticate(ctx, slug, "ws_sr_padron_a10", config.Certificate, config.PrivateKey)
    if err != nil { return nil, err }

    persona, err := s.padron.GetPersona(ctx, ta, parseCUIT(cuit))
    if err != nil { return nil, err }

    // 3. Guardar en cache
    s.repo.UpsertPadronCache(ctx, cuit, persona)
    return persona, nil
}
```

### Frontend

En los formularios de creacion de entidad (cliente/proveedor), al escribir el
CUIT y tab-out (blur), se autoconsulta el padron y se autocompletan:
- Razon social / nombre
- Domicilio fiscal
- Condicion IVA
- Actividad principal

---

## Resumen

| Fase | Scope | Tablas | Migrations |
|---|---|---|---|
| 0 — WSAA + config | Autenticacion + certificados | 1 (afip_config) | 035 |
| 1 — WSFE + CAE | Factura electronica | 1 (afip_log) | 036 |
| 2 — CITI + SIFERE | Exportaciones impositivas | 1 (tax_exports) | 037 |
| 3 — Padron | Consulta contribuyentes | 1 (padron_cache) | 038 |
| **Total** | | **4 tablas** | **4 migrations** |

## Post-plan: que queda reutilizable

- **pkg/afip/** — package completo para integracion AFIP. Reutilizable por
  cualquier servicio que necesite interactuar con AFIP (payroll futuro, etc.)
- **Patron cache de webservice externo** — aplicable a otros servicios
  gubernamentales (ANSES, SRT, etc.)
- **CITI/SIFERE generators** — extensible con nuevos formatos impositivos

## Consideraciones

- **Certificado AFIP:** cada empresa necesita tramitar su certificado digital
  ante AFIP. El proceso es manual y tarda 1-3 dias habiles.
- **Ambiente homologacion:** AFIP tiene un sandbox completo. Todo este plan se
  desarrolla y testea contra homologacion antes de apuntar a produccion.
- **Rate limits:** AFIP no documenta rate limits oficiales, pero en la practica
  rechaza mas de ~50 requests/minuto. El circuit breaker en `pkg/afip/client.go`
  maneja esto.
- **Downtime AFIP:** El webservice se cae regularmente. Las facturas quedan en
  `draft` hasta que AFIP responda. El frontend muestra el estado claramente.
