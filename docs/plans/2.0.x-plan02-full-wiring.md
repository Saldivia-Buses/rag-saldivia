# Plan 02 — Full Wiring (actualizado)

> **Prerequisito:** Plan 01 (spec) implementado al 90%
> **Sprint:** think > plan > execute > review > test > ship
> **Servicios afectados:** auth (backend), web (frontend)
> **Fecha original:** 2026-04-03
> **Fecha actualizacion:** 2026-04-03

---

## Contexto

El plan original tenia 11 fases y cubria desde "el frontend no hace ni un
fetch" hasta Cloudflare Tunnel. El 70% ya esta implementado:

- API client completo con auto-refresh, streaming, tenant resolution
- Auth flow completo (login, refresh, logout, HttpOnly cookies)
- TanStack Query provider configurado
- Chat page 100% wired (sessions CRUD, messages, RAG streaming con thinking)
- Dashboard wired (sessions recientes, notification count, modulos)
- Notifications page wired (list, mark read, mark all read)
- Sidebar dinamico (useEnabledModules desde backend)
- Module system (ModuleGuard, registry, hooks)
- Login app separada (`apps/login/`) funcional
- WebSocket manager (connect, reconnect, heartbeat, subscribe)
- WS Provider con invalidation de `modules` y `notifications`
- Observability (OTel en servicios Go, Grafana stack en compose)
- Cloudflare Tunnel configurado

Este plan reescrito cubre SOLO lo que falta.

---

## Scope

**Servicios:** auth (Go), web (Next.js)
**Archivos nuevos:**
- `apps/web/src/hooks/use-update-profile.ts`
- `apps/web/src/hooks/use-notification-preferences.ts`
- `apps/web/src/components/notification-preferences.tsx`

**Archivos modificados:**
- `services/auth/internal/handler/auth.go` (agregar UpdateMe handler)
- `services/auth/internal/service/auth.go` (agregar UpdateMe method)
- `services/auth/db/queries/auth.sql` (agregar UpdateUserName query)
- `services/auth/internal/repository/` (regenerar con sqlc)
- `services/auth/internal/handler/auth_test.go` (tests para UpdateMe)
- `services/auth/cmd/main.go` (registrar ruta PATCH)
- `apps/web/src/components/settings-profile4.tsx` (wire a backend)
- `apps/web/src/app/(core)/settings/page.tsx` (pasar datos reales)
- `apps/web/src/app/(core)/system-settings/page.tsx` (localStorage persistence)
- `apps/web/src/components/search-command.tsx` (lista dinamica)
- `apps/web/src/lib/ws/provider.tsx` (agregar chat.messages invalidation)
- `apps/web/src/app/(core)/notifications/page.tsx` (link a preferences)

**Migrations:** No. La tabla `users` ya tiene columna `name` (TEXT NOT NULL).
**NATS events:** Ninguno nuevo.
**Tests:** handler test para PATCH /v1/auth/me
**Fuera de scope:**
- API client, auth flow, TanStack Query (ya implementado)
- Login app (ya existe)
- Chat, Dashboard, Notifications pages (ya wired)
- Sidebar dinamico (ya wired)
- Module manifests YAML, OpenAPI specs (es documentacion, no wiring)
- CLI, MCP (otro plan)
- gRPC protos (futuro)
- Observability, Cloudflare Tunnel (ya configurados)

---

## Fases

### Fase 1: Backend — PATCH /v1/auth/me

**Servicio:** auth
**Archivos:**
- `services/auth/db/queries/auth.sql`
- `services/auth/internal/repository/` (regenerado)
- `services/auth/internal/service/auth.go`
- `services/auth/internal/handler/auth.go`
- `services/auth/internal/handler/auth_test.go`
- `services/auth/cmd/main.go`

**Cambios:**

1. **sqlc query** — Agregar a `services/auth/db/queries/auth.sql`:
```sql
-- name: UpdateUserName :exec
UPDATE users SET name = $2, updated_at = now()
WHERE id = $1 AND is_active = true;
```

2. **Regenerar sqlc** — `cd services/auth && sqlc generate`

3. **Service method** — Agregar a `services/auth/internal/service/auth.go`:
```go
// UpdateProfileRequest holds allowed profile updates.
type UpdateProfileRequest struct {
    Name string
}

// UpdateProfile updates the authenticated user's profile.
// Only name is editable — email changes require verification flow (future).
func (a *Auth) UpdateProfile(ctx context.Context, userID string, req UpdateProfileRequest) (*UserInfo, error) {
    name := strings.TrimSpace(req.Name)
    if name == "" {
        return nil, fmt.Errorf("name is required")
    }
    if len(name) > 200 {
        return nil, fmt.Errorf("name too long")
    }

    err := a.repo.UpdateUserName(ctx, repository.UpdateUserNameParams{
        ID:   userID,
        Name: name,
    })
    if err != nil {
        return nil, fmt.Errorf("update profile: %w", err)
    }

    a.auditor.Write(ctx, audit.Entry{
        UserID: userID, Action: "user.profile_updated",
        Details: map[string]any{"name": name},
    })

    return a.Me(ctx, userID)
}
```

4. **Interface** — Agregar a `AuthService` interface en `handler/auth.go`:
```go
UpdateProfile(ctx context.Context, userID string, req service.UpdateProfileRequest) (*service.UserInfo, error)
```

5. **Handler** — Agregar a `services/auth/internal/handler/auth.go`:
```go
// UpdateMe handles PATCH /v1/auth/me
func (h *Auth) UpdateMe(w http.ResponseWriter, r *http.Request) {
    r.Body = http.MaxBytesReader(w, r.Body, 1<<20)
    userID := r.Header.Get("X-User-ID")
    if userID == "" {
        writeJSON(w, http.StatusUnauthorized, errorResponse{Error: "not authenticated"})
        return
    }

    var req struct {
        Name string `json:"name"`
    }
    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
        writeJSON(w, http.StatusBadRequest, errorResponse{Error: "invalid request body"})
        return
    }

    svc, err := h.resolveService(r)
    if err != nil {
        writeJSON(w, http.StatusBadGateway, errorResponse{Error: "tenant not available"})
        return
    }

    user, err := svc.UpdateProfile(r.Context(), userID, service.UpdateProfileRequest{Name: req.Name})
    if err != nil {
        writeJSON(w, http.StatusBadRequest, errorResponse{Error: err.Error()})
        return
    }

    writeJSON(w, http.StatusOK, user)
}
```

6. **Ruta** — Agregar a `services/auth/cmd/main.go` dentro del grupo protegido:
```go
r.Patch("/v1/auth/me", authHandler.UpdateMe)
```

7. **Mock en test** — Agregar a `mockAuthService` en `auth_test.go`:
```go
func (m *mockAuthService) UpdateProfile(_ context.Context, _ string, req service.UpdateProfileRequest) (*service.UserInfo, error) {
    if m.meErr != nil {
        return nil, m.meErr
    }
    return m.userInfo, nil
}
```

8. **Tests** — Agregar a `services/auth/internal/handler/auth_test.go`:
- `TestUpdateMe_Success` — PATCH con `{"name":"Nuevo Nombre"}` + X-User-ID header, espera 200 con UserInfo
- `TestUpdateMe_NoAuth` — PATCH sin X-User-ID, espera 401
- `TestUpdateMe_EmptyName` — PATCH con `{"name":""}`, espera 400
- `TestUpdateMe_InvalidJSON` — PATCH con body malformado, espera 400

**Verificacion:**
- [ ] `cd services/auth && sqlc generate` sin errores
- [ ] `make build-auth` compila
- [ ] `make test-auth` green (incluye nuevos tests)
- [ ] `make lint` clean
- [ ] `curl -X PATCH localhost:8001/v1/auth/me -H "Authorization: Bearer <token>" -d '{"name":"Test"}' devuelve UserInfo actualizado

**Commit:** `feat(auth): add PATCH /v1/auth/me endpoint for profile updates`

---

### Fase 2: Settings Profile — wire a backend

**Servicio:** web (frontend)
**Archivos:**
- `apps/web/src/hooks/use-update-profile.ts` (nuevo)
- `apps/web/src/components/settings-profile4.tsx` (modificar)
- `apps/web/src/app/(core)/settings/page.tsx` (modificar)

**Cambios:**

1. **Hook** — Crear `apps/web/src/hooks/use-update-profile.ts`:
```typescript
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { api } from "@/lib/api/client";
import { useAuthStore } from "@/lib/auth/store";

interface UpdateProfilePayload {
  name: string;
}

export function useUpdateProfile() {
  const queryClient = useQueryClient();
  const fetchMe = useAuthStore((s) => s.fetchMe);

  return useMutation({
    mutationFn: (data: UpdateProfilePayload) =>
      api.patch("/v1/auth/me", data),
    onSuccess: async () => {
      // Refresh the auth store user data
      await fetchMe();
      // Invalidate any cached user queries
      queryClient.invalidateQueries({ queryKey: ["user"] });
    },
  });
}
```

2. **Reescribir settings-profile4.tsx** — Cambiar de componente estatico a wired:
- Importar `useAuthStore` para obtener datos actuales del usuario
- Usar `useState` con los datos reales del usuario (no defaultValues hardcoded)
- El form de "Personal Info" muestra name (editable) y email (readonly, con nota "contactar admin para cambiar")
- Remover secciones irrelevantes para v1: social links, bio, phone, location, website, username (el usuario tiene `name` y `email`, nada mas)
- Simplificar a 2 secciones: "Informacion personal" (name, email) y "Notificaciones" (link a fase 5)
- Boton "Guardar cambios" llama `useUpdateProfile` mutation
- Mostrar loading state en boton, error toast en fallo, success toast en exito
- Boton "Cancelar" resetea el form al estado original

3. **Actualizar page.tsx** — Pasar los datos correctos si es necesario (el componente ahora lee de useAuthStore directamente).

**Verificacion:**
- [ ] Abrir `/settings` — se ven datos reales del usuario (de /v1/auth/me)
- [ ] Cambiar nombre, guardar — request PATCH sale a backend
- [ ] Backend devuelve 200, nombre se actualiza en la UI
- [ ] Cancelar — form vuelve al estado original
- [ ] Email mostrado pero no editable

**Commit:** `feat(web): wire settings profile to PATCH /v1/auth/me`

---

### Fase 3: System Settings — localStorage persistence

**Servicio:** web (frontend)
**Archivos:**
- `apps/web/src/app/(core)/system-settings/page.tsx` (modificar)

**Cambios:**

1. **Language** — Agregar `onValueChange` al Select que guarda en `localStorage`:
```typescript
const [language, setLanguage] = useState(() =>
  typeof window !== "undefined" ? localStorage.getItem("sda-language") ?? "es" : "es"
);

// onValueChange: save to localStorage
const handleLanguageChange = (val: string) => {
  setLanguage(val);
  localStorage.setItem("sda-language", val);
};
```

2. **Timezone** — Igual que language:
```typescript
const [timezone, setTimezone] = useState(() =>
  typeof window !== "undefined"
    ? localStorage.getItem("sda-timezone") ?? "America/Argentina/Buenos_Aires"
    : "America/Argentina/Buenos_Aires"
);
```

3. **Select components** — Cambiar `defaultValue` a `value` y agregar `onValueChange` handler.

**Nota de diseno:** Language y timezone se guardan en localStorage, no en backend. Para v1 esto es suficiente. Cuando haya i18n real (react-intl o similar), la preference de language alimentara al provider. El timezone se usara para formatear fechas en el frontend.

**Verificacion:**
- [ ] Cambiar idioma, recargar pagina — se mantiene el valor
- [ ] Cambiar timezone, recargar — se mantiene
- [ ] Dark mode sigue funcionando (ya usa localStorage)
- [ ] No hay llamadas al backend

**Commit:** `feat(web): persist language and timezone in localStorage`

---

### Fase 4: Search Command — lista dinamica

**Servicio:** web (frontend)
**Archivos:**
- `apps/web/src/components/search-command.tsx` (modificar)

**Cambios:**

1. **Reemplazar `pages` hardcoded** — En vez del array estatico de 6 items, generar la lista combinando core pages + modulos habilitados:
```typescript
import { MODULE_REGISTRY } from "@/lib/modules/registry";
import { useEnabledModules } from "@/lib/modules/hooks";
```

2. **Core pages** — Siempre visibles, hardcoded en el componente (no existe CORE_NAV_ITEMS exportado, los core items estan inline en sidebar6.tsx):
```typescript
const corePages = [
  { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard },
  { label: "Chat", href: "/chat", icon: MessageSquare },
  { label: "Notificaciones", href: "/notifications", icon: Bell },
  { label: "Mi cuenta", href: "/settings", icon: Settings },
  { label: "Configuracion", href: "/system-settings", icon: SlidersHorizontal },
];
```

3. **Module items** — Generados dinamicamente desde `useEnabledModules()` cruzado con `MODULE_REGISTRY`:
```typescript
const { data: enabledModules } = useEnabledModules();
const modulePages = (enabledModules ?? [])
  .map((m) => MODULE_REGISTRY[m.id])
  .filter(Boolean)
  .map((manifest) => ({
    label: manifest.nav.label,
    href: manifest.nav.path,
    icon: manifest.nav.icon,
  }));
```

4. **Combinar** — `const pages = [...corePages, ...modulePages]`

5. **Quitar import de TruckIcon** (ya no necesario, viene del registry si fleet esta habilitado)

**Verificacion:**
- [ ] Ctrl+K abre el search command
- [ ] Se ven las core pages + modulos habilitados del tenant
- [ ] Si un modulo esta deshabilitado, no aparece en la lista
- [ ] Buscar "Chat" filtra correctamente
- [ ] Enter navega a la pagina seleccionada

**Commit:** `feat(web): dynamic search command from enabled modules`

---

### Fase 5: WebSocket — chat message invalidation

**Servicio:** web (frontend)
**Archivos:**
- `apps/web/src/lib/ws/provider.tsx` (modificar)

**Cambios:**

1. **Extender el handler existente de `notifications`** — El chat service publica
mensajes nuevos via `pkg/nats.Notify()` con type `chat.new_message` y payload
`{ session_id, message_id }`. Estos llegan al browser como channel `notifications`
(el Hub hace bridge de `tenant.*.notify.>` a `notifications`).

El WS provider ya subscribe a `notifications` y invalida `["notifications"]`.
Agregar logica para TAMBIEN invalidar chat queries cuando el tipo es chat-related:

```typescript
// Existing notifications handler — MODIFY to also handle chat events
wsManager.subscribe("notifications", (data) => {
  queryClient.invalidateQueries({ queryKey: ["notifications"] });

  // If the notification is a chat event, also invalidate chat data
  const evt = data as { type?: string; data?: string };
  if (evt.type === "chat.new_message" && evt.data) {
    try {
      const parsed = JSON.parse(evt.data);
      if (parsed.session_id) {
        queryClient.invalidateQueries({
          queryKey: ["chat", "messages", parsed.session_id],
        });
      }
    } catch {}
  }
  queryClient.invalidateQueries({ queryKey: ["chat", "sessions"] });
});
```

**No se agrega nueva subscription** — se extiende la existente.

**Verificacion:**
- [ ] Tener 2 tabs abiertas en la misma sesion de chat
- [ ] Enviar mensaje en tab A — tab B recibe el mensaje via WS invalidation
- [ ] La lista de sesiones en la sidebar tambien se actualiza

**Commit:** `feat(web): invalidate chat queries on websocket message events`

---

### Fase 6: Notification Preferences UI

**Servicio:** web (frontend)
**Archivos:**
- `apps/web/src/hooks/use-notification-preferences.ts` (nuevo)
- `apps/web/src/components/notification-preferences.tsx` (nuevo)
- `apps/web/src/app/(core)/notifications/page.tsx` (modificar — agregar link/tab)

**Cambios:**

1. **Hook** — Crear `apps/web/src/hooks/use-notification-preferences.ts`:
```typescript
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { api } from "@/lib/api/client";

export interface NotificationPreferences {
  email_enabled: boolean;
  in_app_enabled: boolean;
  quiet_start: string | null;
  quiet_end: string | null;
  muted_types: string[];
}

export function useNotificationPreferences() {
  return useQuery({
    queryKey: ["notifications", "preferences"],
    queryFn: () => api.get<NotificationPreferences>("/v1/notifications/preferences"),
  });
}

export function useUpdateNotificationPreferences() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: Partial<NotificationPreferences>) =>
      api.put("/v1/notifications/preferences", data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["notifications", "preferences"] });
    },
  });
}
```

2. **Componente** — Crear `apps/web/src/components/notification-preferences.tsx`:
- Usar `useNotificationPreferences()` para cargar datos
- Toggles (Switch de shadcn) para: email_enabled, in_app_enabled
- Inputs de tiempo para quiet hours (quiet_start, quiet_end) — opcional, pueden ser null
- Checkboxes para muted_types (lista de tipos posibles: auth.login_success, chat.new_message, system.announcement)
- Boton "Guardar" llama `useUpdateNotificationPreferences()` mutation
- Loading y error states

3. **Integrar en notifications page** — Agregar un boton/link "Preferencias" en el header de la pagina de notificaciones que abre las preferences (puede ser un dialog, un drawer, o una seccion colapsable debajo del header).

**Backend ya implementado:**
- `GET /v1/notifications/preferences` (handler en `services/notification/internal/handler/notification.go` linea 112)
- `PUT /v1/notifications/preferences` (handler en linea 132)
- El backend acepta: `email_enabled`, `in_app_enabled`, `quiet_start`, `quiet_end`, `muted_types`

**Verificacion:**
- [ ] Ir a `/notifications` — boton "Preferencias" visible
- [ ] Click abre las preferences
- [ ] Toggles reflejan el estado actual (GET preferences)
- [ ] Cambiar un toggle y guardar — PUT sale al backend
- [ ] Recargar — cambios persisten

**Commit:** `feat(web): notification preferences UI wired to backend`

---

## Checklist de scope drift

Despues de cada fase:
- [ ] Solo archivos planeados tocados
- [ ] No dependencias nuevas no planeadas
- [ ] Tests cubren los cambios
- [ ] Tenant isolation verificada si toca DB (Fase 1: el query usa user_id del JWT, no del body)
- [ ] No features fuera del scope

---

## Que NO esta en este plan (ya implementado o es otro plan)

| Item | Estado |
|---|---|
| API client (`apps/web/src/lib/api/client.ts`) | Implementado |
| Auth flow (login, refresh, logout, tokens) | Implementado |
| TanStack Query provider | Implementado |
| Chat page (sessions, messages, RAG streaming) | Implementado |
| Dashboard (sessions, notifications, modules) | Implementado |
| Notifications page (list, mark read) | Implementado |
| Sidebar dinamico (useEnabledModules) | Implementado |
| Module system (guard, registry, hooks) | Implementado |
| Login app separada (`apps/login/`) | Implementado |
| WebSocket manager + provider (base) | Implementado |
| Observability (OTel, Grafana) | Implementado |
| Cloudflare Tunnel | Implementado |
| Module manifests YAML | Docs, no wiring |
| OpenAPI specs | Docs, no wiring |
| gRPC protos | Futuro |
| CLI (`sda`) | Otro plan |
| MCP Server | Otro plan |
