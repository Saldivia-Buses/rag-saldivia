# Plan 26 — Production Hardening & Config Unification

> **Estado:** COMPLETADO — 2026-04-01
> **Branch:** 1.0.x
> **Líneas originales:** ~638 → comprimido a resumen post-ejecución
> **Fuente:** `analisis/18-benchmark-externo.md` + `analisis/19-sota-saldivia-rag.md`

---

## Qué se hizo

Hardening de producción: 0 features nuevas, solo consolidación. Centralización de 17 constantes dispersas, SQLite PRAGMAs, standalone build, security headers, auth token rotation, caching, loading skeletons, error boundaries, y documentación de producción.

### Principio

0 features nuevas. Hardening + unificación de lo existente.

### Fases ejecutadas (9 fases)

| Fase | Qué | Commit |
|------|-----|--------|
| F1 | Centralizar 17 constantes en `packages/config/src/constants.ts` | `23bacda` |
| F2 | SQLite PRAGMAs (WAL, foreign_keys, busy_timeout) + tsconfig ES2023 | `48bcc2d` |
| F3 | Next.js standalone output + compression + security headers | `3ecb71b` |
| F4 | Redis hardening: memory limits, persistence, docker-compose | `acea956` |
| F5 | Access + refresh token rotation con revocación | `eeae933` |
| F6 | Loading skeletons y error boundaries para todas las rutas | `d6339ac` |
| F7 | Cache-Control headers, React.memo, admin stats Redis cache | `903df20` |
| F8 | Remove dead code y dependencias sin uso | `9787ebc` |
| F9 | Production env docs + SQLite backup script | `26c821a` |

### Archivos modificados (21)

**Config:** `packages/config/src/constants.ts` (nuevo), imports actualizados en 6 archivos
**Infra:** `packages/db/src/connection.ts`, `apps/web/next.config.ts`, `docker-compose.yml`
**Auth:** `jwt.ts` (access+refresh split), login/refresh/logout routes (rotation)
**Perf:** collections route (Cache-Control), admin page (Redis cache), componentes (React.memo)
**UX:** Loading skeletons para todas las rutas, error boundaries
**Ops:** `scripts/backup-sqlite.sh`, `docs/production-env.md`

### Resultado

- 17 constantes → 1 archivo central
- JWT: access (15min) + refresh (7d) con rotation y revocación
- Next.js standalone build (Docker-ready)
- Security headers (CSP, HSTS, X-Frame-Options, etc.)
- SQLite con WAL mode + PRAGMAs de producción
- Loading skeletons en todas las rutas
- Error boundaries en todas las rutas
