# Plan de Ejecucion 2.0.5 — Produccion Semana 2026-04-14

> **Fecha:** 2026-04-12
> **Objetivo:** SDA funcionando en produccion con datos migrados de Histrix
> **Deadline:** 2026-04-19 (lunes siguiente)
> **Branch:** `2.0.5`
> **Estado:** Pendiente aprobacion

---

## Resultado de la revision de grupo

Se revisaron los planes 17.5, 18, 19, 20, 21, 22 como grupo. Hallazgos:

| Plan | Estimacion original | Decision | Razon |
|---|---|---|---|
| 17.5 Deploy Reliability | 2 dias | **DONE** | Commit `21be822` |
| 18 ERP Workflows | 6-8 dias | **FULL** | Reconciliacion es daily, QC es clave |
| 19 AFIP Integration | 7-9 dias | **DIFERIDO** | Enzo confirmo que AFIP no es urgente dia 1 |
| 20 BI & Analytics | 5-7 dias | **DIFERIDO** | Reporting puede esperar |
| 21 Data Migration | 7-9 dias | **FULL** (sin AFIP data) | Sin datos migrados no hay nada que operar |
| 22 Frontend Final | 6-8 dias | **PARCIAL** (fases 0+1+5) | Financieras + helpers + polish basico |

### Problemas detectados y resueltos

| # | Problema | Resolucion |
|---|---|---|
| P1 | Plan 21 migration 040 renumerar | Renumerar: Plan 21 usa migration **035** (singular, no 035-038). Plan 19 sera 036-039. |
| P2 | Plan 21 asume Plan 19 completado (AFIP tables) | Skip AFIP data migration. Checklist post-migration: quitar 3 items AFIP. |
| P3 | Las paginas ERP estan bajo `administracion/` no `erp/` | Corregir Plan 22 paths. Actual: `apps/web/src/app/(modules)/administracion/` |
| P4 | Plan 21 no menciona base `saldivia_sa` (multi-empresa) | Investigar si son 2 tenants o mirror. Documentar en Plan 21 |
| P5 | `<Toaster />` de sonner NO esta en root layout | **BLOCKER.** Agregar antes de Plan 22. Todos los toast() son no-ops. |
| P6 | Triggers de Plan 18 vs INSERT de Plan 21 | Plan 18 triggers son on UPDATE/DELETE, no on INSERT. Verificado: no conflictuan. |
| P7 | ~677 tablas legacy, Plan 21 dice ~400 | 677 incluye infra Histrix (HTX_*, oauth_*, etc). ~400 es correcto para negocio |
| P8 | Encoding latin1 en MySQL legacy | Confirmado en schema dump. go-sql-driver `charset=utf8mb4` maneja la conversion |

### Problemas detectados en el review (post-draft)

| # | Severidad | Problema | Resolucion |
|---|---|---|---|
| R1 | BLOCKER | sqlc `CreateFiscalYear` ya existe con params distintos. sqlc falla. | Reemplazar query existente para aceptar `result_account_id` nullable |
| R2 | BLOCKER | No existe query `GetFiscalYear :one`. CloseFiscalYear la necesita. | Agregar query en accounting.sql |
| R3 | BLOCKER | Cross-service dependencies no wired. Invoicing no tiene ref a Accounting/Stock/CurrentAccounts. | Definir interfaces cross-service + actualizar constructors + main.go wiring |
| R4 | BLOCKER | ~20 queries sqlc de fases 1-4 referenciadas en pseudocode pero sin SQL | Escribir SQL completo para cada query ANTES de implementar |
| R5 | HIGH | Permisos nuevos no asignados a rol admin. Admin no puede acceder. | Agregar `INSERT INTO role_permissions` en cada migration |
| R6 | HIGH | Cero `.down.sql` para migrations 030-035 | Escribir down migration para cada up |
| R7 | HIGH | `ListFiscalYears`/`ListTreasuryMovements` no retornan columnas nuevas | Actualizar queries existentes post-migration |
| R8 | HIGH | `erp_prevent_financial_mutation()` permite `cancelled` en journal entries | Agregar guard: entries solo se reversan, no se cancelan directamente |
| R9 | HIGH | `ApiError` no preserva response body. `permissionErrorToast` no funciona. | Extender ApiError con campo `body` |
| R10 | HIGH | Plan 22 estimaciones de lineas infladas x5 (contable: 217 real vs 973 plan) | Buena noticia: hay mas buffer. Corregir estimaciones. |
| R11 | HIGH | Inventario tabs incorrecto. Tesoreria tiene 3 (no 5), Facturacion 2 (no 3). | Mover Caja/Arqueos/Libro IVA a "tabs a agregar" |
| R12 | MEDIUM | Stock.CreateMovement necesita variante TX-aware para QC | Agregar `CreateMovementTx(ctx, tx, ...)` |
| R13 | MEDIUM | Paginas ERP usan wsManager.subscribe() directo — doble invalidation con WsProvider | Remover subscriptions manuales al migrar a TQ |
| R14 | MEDIUM | Plan no menciona paso `make sqlc` entre migration y codigo Go | Agregar paso explicito en orden de ejecucion |
| R15 | MEDIUM | `result_account_id` nullable sin CHECK a nivel DB | Agregar `CHECK (status = 'open' OR result_account_id IS NOT NULL)` |
| R16 | MEDIUM | `CreateReceipt` balance validation sin error type/message/status code | Definir `ErrReceiptUnbalanced`, retornar 400 con diferencia |
| R17 | MEDIUM | Falta query `GetPurchaseReceipt :one` para flujo QC (solo existe List) | Agregar query en purchasing.sql |
| R18 | MEDIUM | `erp_bank_statement_lines.reference` es nullable — deberia ser `NOT NULL DEFAULT ''` | Corregir en migration 031 para consistencia con resto del schema |
| R19 | MEDIUM | Sin response shapes documentadas para ~20 endpoints nuevos. Frontend TQ necesita tipos. | Documentar response shapes en Plan 18 o en types.ts de Plan 22 |
| R20 | LOW | `@faker-js/faker` en dependencies de produccion (~3MB al bundle cliente) | Mover a devDependencies |
| R21 | LOW | Plan 22 prerequisite dice "Plan 17+18+19+20 completados" — contradice ejecucion parcial | Agregar nota: fases 0+1+5 son validas sin Plan 19/20 |
| R22 | LOW | `permission-messages.ts` incluye permisos AFIP que no existen sin Plan 19 | Harmless (dead entries en lookup map), documentar como intencional |
| R23 | LOW | `ExportButtons` componente depende de `pkg/export` de Plan 20 (diferido) | No esta en scope 2.0.5 fases 0+1+5. Verificado correcto. |

---

## Orden de ejecucion

### Pre-requisitos (antes de dia 1)

1. Resolver R1-R4 (blockers): escribir SQL completo de ~20 queries, definir interfaces
   cross-service, arreglar CreateFiscalYear name collision, agregar GetFiscalYear
2. Resolver R5: agregar `INSERT INTO role_permissions` para cada permiso nuevo
3. Resolver R6: escribir `.down.sql` para cada migration

### Ejecucion

```
DIA 1-2:  Plan 18 Fases 0+2      (cierre fiscal + anulacion cascada)
          → make sqlc despues de cada migration
DIA 2-3:  Plan 18 Fases 1+3      (reconciliacion + QC recepcion)
          → hacer sqlc, actualizar queries existentes (R7)
DIA 3-4:  Plan 18 Fase 4          (recibos cobro/pago)
          → incluir cross-service wiring en main.go (R3)
DIA 4-5:  Plan 21 Fases 0+1      (infra migration + pre-validation)
DIA 5-6:  Plan 21 Fases 2-5      (migrar datos + validar)
DIA 6:    Plan 22 Fix Toaster     (agregar <Toaster /> al root layout) (P5)
DIA 6-7:  Plan 22 Fases 0+1+5    (helpers + paginas financieras + polish)
          → extender ApiError.body (R9), remover wsManager manual (R13)
```

### Dependencias

```
Plan 18 (migrations 030-034) ──┐
                                ├──► Plan 21 (migrations 035-038)
Plan 17 (ya completado) ───────┘           │
                                            └──► Plan 22 parcial
```

---

## Bloque 1: Plan 18 — ERP Workflows (dias 1-4)

### Fase 0 — Cierre de ejercicio contable

**Migration:** `db/tenant/migrations/030_erp_fiscal_close.up.sql`
**Archivos Go:**
- `services/erp/internal/handler/accounting.go` — agregar endpoints close/preview
- `services/erp/internal/service/accounting.go` — logica CloseFiscalYear
- `services/erp/db/queries/accounting.sql` — agregar queries ListDraftEntriesInPeriod, GetAccountBalancesForClose, CloseFiscalYear, CreateFiscalYear

**Endpoints nuevos:**
| Method | Path | Permiso |
|---|---|---|
| PATCH | `/v1/erp/accounting/fiscal-years/{id}/result-account` | `erp.accounting.close` |
| POST | `/v1/erp/accounting/fiscal-years/{id}/close` | `erp.accounting.close` |
| GET | `/v1/erp/accounting/fiscal-years/{id}/preview-close` | `erp.accounting.read` |

**Frontend:** Boton "Cerrar ejercicio" en tab Ejercicios de contable.
**NATS:** `tenant.{slug}.notify.erp_fiscal_close`, `tenant.{slug}.erp_accounting`

---

### Fase 1 — Reconciliacion bancaria

**Migration:** `db/tenant/migrations/031_erp_reconciliation.up.sql`
- Tabla `erp_bank_reconciliations`
- Tabla `erp_bank_statement_lines`
- ALTER `erp_treasury_movements` (add reconciled, reconciliation_id)
- Trigger treasury-specific `erp_prevent_treasury_mutation()`

**Archivos Go:**
- `services/erp/internal/handler/treasury.go` — agregar endpoints reconciliation
- `services/erp/internal/service/treasury.go` — AutoMatch, ConfirmReconciliation
- `services/erp/db/queries/treasury.sql` — queries reconciliation

**Endpoints nuevos:** 7 (ver plan 18 fase 1)
**Frontend:** Tab "Reconciliacion" en tesoreria. Layout split extracto vs movimientos.

---

### Fase 2 — Anulacion en cascada

**Migration:** `db/tenant/migrations/032_erp_cascade_void.up.sql`
- Reemplazar `erp_prevent_financial_mutation()` para permitir transitions controladas
- Agregar `reversal` a CHECK de entry_type
- ALTER `erp_invoices` (add voided_by, void_reason)
- ALTER `erp_journal_entries` (add reversed_by)

**Archivos Go:**
- `services/erp/internal/handler/invoicing.go` — VoidInvoice, VoidPreview
- `services/erp/internal/service/invoicing.go` — VoidInvoice (TX atomica, 7 pasos)
- `services/erp/db/queries/invoicing.sql` — queries void

**NOTA CRITICA:** En V1 sin Plan 19, VoidInvoice solo anula facturas SIN CAE.
Facturas con CAE se bloquean hasta que Plan 19 implemente NCA con CAE propio.

**Endpoints nuevos:** 2
**Frontend:** Boton "Anular" en detalle factura con preview.

---

### Fase 3 — Recepcion con control de calidad

**Migration:** `db/tenant/migrations/033_erp_qc_reception.up.sql`
- Tabla `erp_qc_inspections`
- Tabla `erp_supplier_demerits`

**Archivos Go:**
- `services/erp/internal/handler/purchasing.go` — InspectReceipt, ListInspections
- `services/erp/internal/service/purchasing.go` — InspectReceipt (accept/reject + stock + demerits)
- `services/erp/db/queries/purchasing.sql` — queries QC

**Scope V1:** Solo accept/reject. Sin quarantine workflow.
**Endpoints nuevos:** 4
**Frontend:** Tab "Inspecciones" en compras. Badge demerits en ficha proveedor.

---

### Fase 4 — Recibos de cobro y pago

**Migration:** `db/tenant/migrations/034_erp_receipts.up.sql`
- Tabla `erp_receipts`
- Tabla `erp_receipt_payments`
- Tabla `erp_receipt_allocations`
- Tabla `erp_receipt_withholdings`
- Trigger inmutabilidad en receipts

**Archivos Go:**
- `services/erp/internal/handler/treasury.go` — CRUD receipts + void
- `services/erp/internal/service/treasury.go` — CreateReceipt (TX: treasury + contabilidad + cta corriente)
- `services/erp/db/queries/treasury.sql` — queries receipts

**Endpoints nuevos:** 4
**Frontend:** Tabs "Cobros"/"Pagos" en tesoreria.

**Resumen Plan 18:**
- 5 migrations (030-034)
- 8 tablas nuevas + 3 ALTERs
- ~20 endpoints nuevos
- 5 permisos nuevos
- 5 NATS events nuevos

---

## Bloque 2: Plan 21 — Data Migration (dias 4-6)

### Ajustes al plan original

1. **Renumerar migration:** 035 (singular, no 035-038) ya que Plan 19/20 estan diferidos. Plan 19 sera 036-039 cuando se implemente.
2. **Skip AFIP data:** No migrar a tablas AFIP (no existen todavia)
3. **Agregar referencia a scrape:** `.intranet-scrape/db-schema.sql` tiene el DDL completo,
   `.intranet-scrape/db-tables.txt` tiene la lista de 677 tablas
4. **Multi-empresa `saldivia_sa`:** Investigar si los datos de `saldivia` y `saldivia_sa`
   son dos tenants o mirror. Los triggers de sync sugieren mirror → migrar solo `saldivia`.
5. **Checklist post-migration ajustado (sin AFIP):**
   - [ ] Verificar counts por dominio
   - [ ] Verificar sumas de control financieras
   - [ ] Setear `result_account_id` en fiscal years abiertos
   - [ ] Run de cascade void sobre factura draft de prueba
   - [ ] Verificar reconciliacion con datos reales

### Fase 0 — Infra de migracion

**Migration:** `db/tenant/migrations/035_erp_legacy_mapping.up.sql`
- Tabla `erp_legacy_mapping` (INT→UUID mapping)
- Tabla `erp_migration_runs`
- Tabla `erp_migration_table_progress`
- Tabla `erp_migration_validation_issues`

**Archivos Go (CLI):**
- `tools/cli/cmd/migrate_legacy.go`
- `tools/cli/internal/legacy/connection.go`
- `tools/cli/internal/legacy/reader.go`
- `tools/cli/internal/migration/orchestrator.go`
- `tools/cli/internal/migration/mapper.go`
- `tools/cli/internal/migration/transformer.go`
- `tools/cli/internal/migration/validator.go`
- `tools/cli/internal/migration/writer.go`

### Fase 1 — Pre-validation

**Queries MySQL** contra constraints Plan 17+18. Reportar filas problematicas.
Ejecutar exclusivamente en dry-run. Bloquear prod run si hay issues `fix_manual`.

### Fase 2-5 — Migrar datos por dominio

| Fase | Dominio | Tablas MySQL principales | Tablas SDA target |
|---|---|---|---|
| 2 | Base | GEN_*, REG_CUENTA, PERSONAL, MED_CONTACTO | erp_catalogs, erp_entities, erp_entity_contacts |
| 3 | Financiero | CTB_*, CAJ_*, CAR_*, FACREMIT, FACDETAL, IVA*, RET*, REG_MOVIMIENTOS | erp_accounts → erp_withholdings (14 tablas) |
| 4 | Operativo | STK_*, DEPOSITO, CPSENCAB, CPS_*, COTIZACION, PEDIDOINT, MRP_* | erp_warehouses → erp_production_orders (12 tablas) |
| 5 | RRHH + validacion | PERSONAL (extendido), licencias, capacitaciones | erp_employee_details, erp_absences, erp_training_records |

**Total:** ~400 tablas legacy → ~34 tablas SDA

---

## Bloque 3: Plan 22 Parcial — Frontend (dias 6-7)

### Fase 0 — Helpers compartidos

**Archivos nuevos:**
- `apps/web/src/lib/erp/format.ts` — fmtMoney, fmtDate, fmtNumber, fmtPercent
- `apps/web/src/lib/erp/queries.ts` — query key factory
- `apps/web/src/lib/erp/types.ts` — tipos TypeScript compartidos

**Path correcto:** Las paginas estan en `apps/web/src/app/(modules)/administracion/`
(no `erp/` como dicen los planes originales).

**Verificar/agregar:** `<Toaster />` de sonner en root layout si falta.

**Agregar WS→TQ bridge:** Handlers ERP en WsProvider existente para invalidation por dominio.

### Fase 1 — Migrar paginas financieras a TanStack Query

| Pagina | Estimacion | Mutations a agregar |
|---|---|---|
| `contable/page.tsx` (~973 lineas) | Grande | CreateEntry, PostEntry, ReverseEntry, CloseFiscalYear |
| `tesoreria/page.tsx` (~929 lineas) | Grande | CreateMovement, CreateCheck, Reconciliation, CreateReceipt |
| `facturacion/page.tsx` (~1022 lineas) | Grande | CreateInvoice, PostInvoice, VoidInvoice |
| `cuentas/page.tsx` | Mediana | Allocate |
| `pagos/page.tsx` | Pequena | Redirect a tesoreria |

**Patron:** useState+useEffect → useQuery/useMutation. Toast en toda mutation.
Borrar codigo viejo, no comentar. Importar helpers de `lib/erp/`.

### Fase 5 (parcial) — Polish minimo

- Tildes correctas en paginas financieras
- Pagination en tablas con >20 items
- Empty states en tablas vacias
- Error states con boton reintentar
- Loading skeletons basicos

---

## Lo que queda para semana 2-3 (post-lanzamiento)

| Semana | Plan | Scope |
|---|---|---|
| 2 | **19 AFIP** | WSAA + WSFE + CAE + CITI + Padron. Migrations se renumeran (036+) |
| 2 | **22 Fases 2-3** | Migrar paginas operativas (almacen, compras, ventas, produccion) |
| 2 | **21 AFIP data** | Migrar certs AFIP + CAE historicos cuando Plan 19 cree las tablas |
| 3 | **20 Analytics** | pkg/export + 37 endpoints + charts + dashboard |
| 3 | **22 Fases 4-5** | Tabs faltantes + create dialogs + responsive + dead code cleanup |

---

## Migration numbering final

| # | Archivo | Plan | Contenido |
|---|---|---|---|
| 030 | `030_erp_fiscal_close.up.sql` | 18.0 | ALTER fiscal_years + permiso close |
| 031 | `031_erp_reconciliation.up.sql` | 18.1 | Reconciliaciones + ALTER treasury |
| 032 | `032_erp_cascade_void.up.sql` | 18.2 | Rewrite financial mutation trigger + void columns |
| 033 | `033_erp_qc_reception.up.sql` | 18.3 | QC inspections + demerits |
| 034 | `034_erp_receipts.up.sql` | 18.4 | Receipts + payments + allocations |
| 035 | `035_erp_legacy_mapping.up.sql` | 21.0 | Migration infra (mapping + runs + progress + validation) |
| --- | --- | --- | --- |
| 036-039 | Reservados para Plan 19 (AFIP) | 19 | afip_config, afip_log, tax_exports, padron_cache |
| 040 | Reservado para Plan 20 (Analytics) | 20 | erp_pending_exports |

**Nota:** Cada migration up tiene su correspondiente `.down.sql` (R6).
**Nota:** Cada migration que agrega permisos incluye `INSERT INTO role_permissions` para admin (R5).

---

## Riesgos y mitigaciones

| Riesgo | Probabilidad | Impacto | Mitigacion |
|---|---|---|---|
| Plan 18 toma mas de 4 dias | Media | Alto | Priorizar fases 0+2+4 (criticas), dejar 1+3 para dia 4 |
| MySQL legacy inaccesible desde dev | Alta | Alto | Enzo confirmo separado. Necesita tunel SSH o VPN a workstation |
| Schema MySQL difiere del esperado | Baja | Alto | Tenemos db-schema.sql completo para verificar offline |
| Data legacy con inconsistencias | Media | Medio | Pre-validation (fase 1 Plan 21) detecta antes de insertar |
| Frontend migration descubre bugs en backend | Media | Medio | Buffer dia 7 para fixes |
| Cross-service wiring es mas complejo de lo esperado | Media | Alto | Definir interfaces minimas, inyectar solo lo necesario por fase |
| Queries sqlc incompletas bloquean implementacion | Alta | Alto | Escribir ALL SQL como pre-requisito dia 0, no inline |

---

## Criterios de exito para produccion

- [ ] Las 6 migrations (030-035) aplicadas con up + down
- [ ] Todos los permisos nuevos asignados a rol admin
- [ ] Contadora puede cerrar ejercicio, anular factura, registrar recibo
- [ ] Tesorero puede reconciliar extracto bancario
- [ ] Compras puede inspeccionar recepcion (accept/reject + demerits)
- [ ] Cross-service TX funcionando (void cascada, recibos multi-dominio)
- [ ] Datos historicos de Histrix migrados y verificados (counts + sumas de control)
- [ ] Paginas financieras con TanStack Query, toast en mutations, error/empty states
- [ ] `<Toaster />` funcionando — toast visible al crear/anular/cerrar
- [ ] `make versions` muestra MATCH en todos los servicios
- [ ] Histrix queda read-only como referencia

---

## Resumen del review

**3 revisores ejecutados:** gateway-reviewer (backend), frontend-reviewer, cross-plan (Opus).

| Severidad | Cantidad | Estado |
|---|---|---|
| BLOCKER | 5 | R1-R4 + P5. Resolver como pre-requisito. |
| HIGH | 9 | R5-R11 + H1-H2. Resolver durante implementacion. |
| MEDIUM | 8 | R12-R19. Resolver durante implementacion. |
| LOW | 3 | R20-R23. Resolver oportunisticamente. |
| **Total** | **25** | Todos documentados. |

**Conclusion:** El plan es arquitecturalmente solido. Las FK dependencies son correctas,
los packages compartidos existen, los patterns NATS son correctos. Los blockers son todos
resolubles (queries faltantes, wiring, toaster). Las paginas son mas chicas de lo esperado
(~200 lineas vs ~900 estimadas), lo que da buffer extra en dia 6-7.
