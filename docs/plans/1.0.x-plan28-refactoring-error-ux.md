# Plan 28 — Security Fixes + Decomposition + Error UX

> **Estado:** COMPLETADO — 2026-04-01
> **Branch:** 1.0.x
> **Líneas originales:** ~222 → comprimido a resumen post-ejecución
> **Fuente:** `analisis/16-rendimiento.md`, `analisis/17-riesgos-y-deuda-tecnica.md`, `analisis/18-benchmark-externo.md`

---

## Qué se hizo

Security fixes, decomposition de 3 componentes gigantes, y sistema de error feedback para usuarios. Derivado del análisis completo del codebase.

### Principio

Security primero (biblia: "no es un tradeoff"), decomposition después, feature nueva (feedback) al final — honestamente etiquetada como tal.

### Fases ejecutadas (6 fases + review)

| Fase | Commit | Qué |
|------|--------|-----|
| F1-F2 | `c69ff15` | FTS5 MATCH sanitization (wrappea input en comillas) + cifrado AES-256-GCM de `external_sources.credentials` con lazy migration. 11 tests nuevos. |
| F3 | `121a0f5` | ChatInterface 643→360 líneas. Extrajo ChatEmptyState (106) + ChatMessages (160). |
| F4 | `05b521d` | AdminRoles 626→238 líneas. Extrajo RoleCard (137) + RoleForm (184). Eliminó ~150 líneas de card duplicada. |
| F5 | `d4e95da` | AdminUsers 592→260 líneas. Extrajo CreateUserForm (92) + PasswordResetCell (137). Encapsuló máquina de estados de password reset. Removió dead code `handleChangeRole`. |
| F6 | `0b289b0` | Error feedback: `lib/error-feedback.ts` + `ui/error-feedback.tsx` + `POST /api/feedback`. Guarda en `audit_log` con action="error_feedback". |
| Review | `acef2ec` | Conectó feedback a AdminRoles (3 catches) y AdminUsers (2 catches). Removió dead try/catch en decrypt. Montó ErrorFeedbackMount en Providers. |

### Archivos creados (9)

```
apps/web/src/lib/error-feedback.ts              — reportError() + showErrorFeedback()
apps/web/src/components/ui/error-feedback.tsx    — ErrorFeedbackMount + dialog
apps/web/src/app/api/feedback/route.ts           — POST endpoint
apps/web/src/components/chat/ChatMessages.tsx    — extraído de ChatInterface
apps/web/src/components/chat/ChatEmptyState.tsx  — extraído de ChatInterface
apps/web/src/components/admin/RoleCard.tsx       — extraído de AdminRoles
apps/web/src/components/admin/RoleForm.tsx       — extraído de AdminRoles
apps/web/src/components/admin/CreateUserForm.tsx — extraído de AdminUsers
apps/web/src/components/admin/PasswordResetCell.tsx — extraído de AdminUsers
```

### Resultado

- 2 security fixes con 11 tests
- 3 componentes decomposed: 1,861 → 858 líneas totales (-54%)
- 7 sub-componentes nuevos, cada uno con responsabilidad única
- Error feedback system con toast + dialog + endpoint
- 0 type errors, 0 test failures, 0 lint warnings
