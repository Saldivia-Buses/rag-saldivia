# Mission Brief — 2026-03-24

## 🔭 INTEL

**Búsqueda:** RAG configuration UI admin panel best practices 2025

**Hallazgos clave:**
- Patrón dominante: 3 capas — YAML base (persistente), DB por usuario (preferencias), session state (efímero)
- Open WebUI: Admin Settings > Models para defaults globales, Advanced Parameters por usuario para overrides
- SMARTFinRAG: YAML config + overrides en runtime via UI — separación limpia base/runtime
- Parámetros estándar en producción: top-k retrieval (1-50), temperatura (0.0-1.0), top-p, max tokens, embedding model, reranker model
- Validación pre-persistencia es crítica para evitar degradación silenciosa de calidad

---

## 📊 SITUATION

**Tests:** 169 passed, 0 failed (12.47s)

**Git log reciente:**
```
5594dcd Merge pull request #17 from Camionerou/fase9/admin-pro
190485d fix(fase9): PRAGMA FK en memoria, delete_area limpia area_collections
b221c48 docs(fase9): actualizar CLAUDE.md, roadmap y CHANGELOG
a88dd21 fix(fase9): labels accesibles, validación create user
cc4eaf6 feat(fase9): system/+page.svelte — stats cards, jobs activos
```

**Estado del sistema:** Fase 9 (Admin Pro) completada y mergeada a main.

**Deuda técnica visible:**
- `rag-config/+page.svelte` es un stub de "próximamente"
- `config.py` no tiene `get_rag_params()`, `update_rag_params()`, `reset_rag_params()`, `switch_profile()`
- 4 parámetros RAG (`top_p`, `top_k`, `vdb_top_k`, `reranker_top_k`) no están en `ENV_MAPPING`

---

## 🎯 MISSION

**Fase 10: Admin RAG Config** — UI para que el admin configure parámetros del RAG engine en runtime.

**Por qué hoy:**
- Fase 9 completada → Fase 10 es la siguiente según roadmap (dependencia directa)
- El stub de rag-config existe pero vacío — visible en sidebar para todos los admins
- Los parámetros como temperatura y top-k son críticos para tuning de calidad de respuestas

**Decisiones tomadas en diseño:**
- Persistencia vía `config/admin-overrides.yaml` gitignoreado (base YAMLs intactos)
- 4 params nuevos en ENV_MAPPING: top_p, top_k, vdb_top_k, reranker_top_k
- Profile switching solo in-memory (no restart automático desde Docker)
- Spec: `docs/superpowers/specs/2026-03-24-fase10-rag-config-design.md`

---

## ⚡ EXECUTION

1. **GATE 3 — Implement (TDD):**
   - Agregar params a ENV_MAPPING + métodos a `config.py` (tests primero)
   - Agregar 4 endpoints a `gateway.py` (tests primero)
   - Crear componentes SvelteKit: ConfigSlider, ModelSelector, GuardrailsToggle, ProfileSwitcher
   - Implementar `+page.server.ts` y `+page.svelte` de rag-config
   - Crear BFF routes `/api/admin/config` y `/api/admin/profile`

2. **GATE 4 — Simplify:** `simplify` skill + dead code check

3. **GATE 5 — Review:** subagente `gateway-reviewer` + `frontend-reviewer`

4. **GATE 6 — Docs:** `doc-writer` + CHANGELOG.md + roadmap update
