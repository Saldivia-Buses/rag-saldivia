---
name: briefing
description: "Show session briefing — recent commits, modified files, uncommitted changes, service versions"
user_invocable: true
---

Run the session briefing:

```bash
bash .claude/hooks/session-briefing.sh
```

Summarize the current state: what branch we're on, what changed recently, what's uncommitted.
